apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: appbackups.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: AppBackup
    listKind: AppBackupList
    plural: appbackups
    singular: appbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The cluster of the AppBackup
      jsonPath: .spec.cluster
      name: Cluster
      type: string
    - description: The total number of backups managed by this AppBackup
      jsonPath: .status.totalBackups
      name: TotalBackups
      type: integer
    - description: The status of the AppBackup
      jsonPath: .status.status
      name: Status
      type: string
    - description: The status of the latest backup
      jsonPath: .status.latestBackupStatus
      name: LatestBackupStatus
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: AppBackup is the Schema for the appbackups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AppBackup
            properties:
              action:
                description: Action defines the manual action to be performed.
                properties:
                  requestAt:
                    description: |-
                      RequestAt is the time when the action was requested.
                      The controller will process the action if this timestamp is newer than the one in status.
                    format: date-time
                    type: string
                  targetBackup:
                    description: |-
                      TargetBackup specifies the name of the backup to operate on.
                      If empty, the action applies to the latest backup (for Retry/Cancel).
                      For Delete action, this field is required.
                    type: string
                  type:
                    description: |-
                      Type is the type of the action.
                      Valid values are: "Backup", "Retry", "Cancel", "Delete".
                    type: string
                required:
                - requestAt
                - type
                type: object
              cluster:
                description: Cluster is the name of the cluster to backup
                type: string
              disasterPolicy:
                description: 策略配置名称
                type: string
              paused:
                description: Paused specifies whether the schedule is paused or not
                type: boolean
              schedule:
                description: |-
                  Schedule is a Cron expression defining when to run
                  the Backup. "@manual" is a special schedule that runs once immediately
                type: string
              skipImmediately:
                description: 跳过首次备份
                type: boolean
              template:
                description: |-
                  Template is the definition of the Backup to be run
                  on the provided schedule
                properties:
                  csiSnapshotTimeout:
                    description: |-
                      CSISnapshotTimeout specifies the time used to wait for CSI VolumeSnapshot status turns to
                      ReadyToUse during creation, before returning error as timeout.
                      The default value is 10 minute.
                    type: string
                  datamover:
                    description: |-
                      DataMover specifies the data mover to be used by the backup.
                      If DataMover is "" or "velero", the built-in data mover will be used.
                    type: string
                  defaultVolumesToFsBackup:
                    description: |-
                      DefaultVolumesToFsBackup specifies whether pod volume file system backup should be used
                      for all volumes by default.
                    nullable: true
                    type: boolean
                  defaultVolumesToRestic:
                    description: |-
                      DefaultVolumesToRestic specifies whether restic should be used to take a
                      backup of all pod volumes by default.

                      Deprecated: this field is no longer used and will be removed entirely in future. Use DefaultVolumesToFsBackup instead.
                    nullable: true
                    type: boolean
                  excludedClusterScopedResources:
                    description: |-
                      ExcludedClusterScopedResources is a slice of cluster-scoped
                      resource type names to exclude from the backup.
                      If set to "*", all cluster-scoped resource types are excluded.
                      The default value is empty.
                    items:
                      type: string
                    nullable: true
                    type: array
                  excludedNamespaceScopedResources:
                    description: |-
                      ExcludedNamespaceScopedResources is a slice of namespace-scoped
                      resource type names to exclude from the backup.
                      If set to "*", all namespace-scoped resource types are excluded.
                      The default value is empty.
                    items:
                      type: string
                    nullable: true
                    type: array
                  excludedNamespaces:
                    description: |-
                      ExcludedNamespaces contains a list of namespaces that are not
                      included in the backup.
                    items:
                      type: string
                    nullable: true
                    type: array
                  excludedResources:
                    description: |-
                      ExcludedResources is a slice of resource names that are not
                      included in the backup.
                    items:
                      type: string
                    nullable: true
                    type: array
                  hooks:
                    description: Hooks represent custom behaviors that should be executed
                      at different phases of the backup.
                    properties:
                      resources:
                        description: Resources are hooks that should be executed when
                          backing up individual instances of a resource.
                        items:
                          description: |-
                            BackupResourceHookSpec defines one or more BackupResourceHooks that should be executed based on
                            the rules defined for namespaces, resources, and label selector.
                          properties:
                            excludedNamespaces:
                              description: ExcludedNamespaces specifies the namespaces
                                to which this hook spec does not apply.
                              items:
                                type: string
                              nullable: true
                              type: array
                            excludedResources:
                              description: ExcludedResources specifies the resources
                                to which this hook spec does not apply.
                              items:
                                type: string
                              nullable: true
                              type: array
                            includedNamespaces:
                              description: |-
                                IncludedNamespaces specifies the namespaces to which this hook spec applies. If empty, it applies
                                to all namespaces.
                              items:
                                type: string
                              nullable: true
                              type: array
                            includedResources:
                              description: |-
                                IncludedResources specifies the resources to which this hook spec applies. If empty, it applies
                                to all resources.
                              items:
                                type: string
                              nullable: true
                              type: array
                            labelSelector:
                              description: LabelSelector, if specified, filters the
                                resources to which this hook spec applies.
                              nullable: true
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            name:
                              description: Name is the name of this hook.
                              type: string
                            post:
                              description: |-
                                PostHooks is a list of BackupResourceHooks to execute after storing the item in the backup.
                                These are executed after all "additional items" from item actions are processed.
                              items:
                                description: BackupResourceHook defines a hook for
                                  a resource.
                                properties:
                                  exec:
                                    description: Exec defines an exec hook.
                                    properties:
                                      command:
                                        description: Command is the command and arguments
                                          to execute.
                                        items:
                                          type: string
                                        minItems: 1
                                        type: array
                                      container:
                                        description: |-
                                          Container is the container in the pod where the command should be executed. If not specified,
                                          the pod's first container is used.
                                        type: string
                                      onError:
                                        description: OnError specifies how Velero
                                          should behave if it encounters an error
                                          executing this hook.
                                        enum:
                                        - Continue
                                        - Fail
                                        type: string
                                      timeout:
                                        description: |-
                                          Timeout defines the maximum amount of time Velero should wait for the hook to complete before
                                          considering the execution a failure.
                                        type: string
                                    required:
                                    - command
                                    type: object
                                required:
                                - exec
                                type: object
                              type: array
                            pre:
                              description: |-
                                PreHooks is a list of BackupResourceHooks to execute prior to storing the item in the backup.
                                These are executed before any "additional items" from item actions are processed.
                              items:
                                description: BackupResourceHook defines a hook for
                                  a resource.
                                properties:
                                  exec:
                                    description: Exec defines an exec hook.
                                    properties:
                                      command:
                                        description: Command is the command and arguments
                                          to execute.
                                        items:
                                          type: string
                                        minItems: 1
                                        type: array
                                      container:
                                        description: |-
                                          Container is the container in the pod where the command should be executed. If not specified,
                                          the pod's first container is used.
                                        type: string
                                      onError:
                                        description: OnError specifies how Velero
                                          should behave if it encounters an error
                                          executing this hook.
                                        enum:
                                        - Continue
                                        - Fail
                                        type: string
                                      timeout:
                                        description: |-
                                          Timeout defines the maximum amount of time Velero should wait for the hook to complete before
                                          considering the execution a failure.
                                        type: string
                                    required:
                                    - command
                                    type: object
                                required:
                                - exec
                                type: object
                              type: array
                          required:
                          - name
                          type: object
                        nullable: true
                        type: array
                    type: object
                  includeClusterResources:
                    description: |-
                      IncludeClusterResources specifies whether cluster-scoped resources
                      should be included for consideration in the backup.
                    nullable: true
                    type: boolean
                  includedClusterScopedResources:
                    description: |-
                      IncludedClusterScopedResources is a slice of cluster-scoped
                      resource type names to include in the backup.
                      If set to "*", all cluster-scoped resource types are included.
                      The default value is empty, which means only related
                      cluster-scoped resources are included.
                    items:
                      type: string
                    nullable: true
                    type: array
                  includedNamespaceScopedResources:
                    description: |-
                      IncludedNamespaceScopedResources is a slice of namespace-scoped
                      resource type names to include in the backup.
                      The default value is "*".
                    items:
                      type: string
                    nullable: true
                    type: array
                  includedNamespaces:
                    description: |-
                      IncludedNamespaces is a slice of namespace names to include objects
                      from. If empty, all namespaces are included.
                    items:
                      type: string
                    nullable: true
                    type: array
                  includedResources:
                    description: |-
                      IncludedResources is a slice of resource names to include
                      in the backup. If empty, all resources are included.
                    items:
                      type: string
                    nullable: true
                    type: array
                  itemOperationTimeout:
                    description: |-
                      ItemOperationTimeout specifies the time used to wait for asynchronous BackupItemAction operations
                      The default value is 4 hour.
                    type: string
                  labelSelector:
                    description: |-
                      LabelSelector is a metav1.LabelSelector to filter with
                      when adding individual objects to the backup. If empty
                      or nil, all objects are included. Optional.
                    nullable: true
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  metadata:
                    properties:
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                    type: object
                  orLabelSelectors:
                    description: |-
                      OrLabelSelectors is list of metav1.LabelSelector to filter with
                      when adding individual objects to the backup. If multiple provided
                      they will be joined by the OR operator. LabelSelector as well as
                      OrLabelSelectors cannot co-exist in backup request, only one of them
                      can be used.
                    items:
                      description: |-
                        A label selector is a label query over a set of resources. The result of matchLabels and
                        matchExpressions are ANDed. An empty label selector matches all objects. A null
                        label selector matches no objects.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    nullable: true
                    type: array
                  orderedResources:
                    additionalProperties:
                      type: string
                    description: |-
                      OrderedResources specifies the backup order of resources of specific Kind.
                      The map key is the resource name and value is a list of object names separated by commas.
                      Each resource name has format "namespace/objectname".  For cluster resources, simply use "objectname".
                    nullable: true
                    type: object
                  resourcePolicy:
                    description: ResourcePolicy specifies the referenced resource
                      policies that backup should follow
                    properties:
                      apiGroup:
                        description: |-
                          APIGroup is the group for the resource being referenced.
                          If APIGroup is not specified, the specified Kind must be in the core API group.
                          For any other third-party types, APIGroup is required.
                        type: string
                      kind:
                        description: Kind is the type of resource being referenced
                        type: string
                      name:
                        description: Name is the name of resource being referenced
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                    x-kubernetes-map-type: atomic
                  snapshotMoveData:
                    description: SnapshotMoveData specifies whether snapshot data
                      should be moved
                    nullable: true
                    type: boolean
                  snapshotVolumes:
                    description: |-
                      SnapshotVolumes specifies whether to take snapshots
                      of any PV's referenced in the set of objects included
                      in the Backup.
                    nullable: true
                    type: boolean
                  storageLocation:
                    description: StorageLocation is a string containing the name of
                      a BackupStorageLocation where the backup should be stored.
                    type: string
                  ttl:
                    description: |-
                      TTL is a time.Duration-parseable string describing how long
                      the Backup should be retained for.
                    type: string
                  uploaderConfig:
                    description: UploaderConfig specifies the configuration for the
                      uploader.
                    nullable: true
                    properties:
                      parallelFilesUpload:
                        description: ParallelFilesUpload is the number of files parallel
                          uploads to perform when using the uploader.
                        type: integer
                    type: object
                  volumeGroupSnapshotLabelKey:
                    description: VolumeGroupSnapshotLabelKey specifies the label key
                      to group PVCs under a VGS.
                    type: string
                  volumeSnapshotLocations:
                    description: VolumeSnapshotLocations is a list containing names
                      of VolumeSnapshotLocations associated with this backup.
                    items:
                      type: string
                    type: array
                type: object
              timeout:
                description: |-
                  Timeout defines the maximum duration to wait for the backup to complete.
                  If exceeded, the controller will attempt to cancel/delete the Velero Backup.
                type: string
              useOwnerReferencesInBackup:
                description: |-
                  UseOwnerReferencesBackup specifies whether to use
                  OwnerReferences on backups created by this Schedule.
                nullable: true
                type: boolean
            required:
            - cluster
            - schedule
            - template
            type: object
          status:
            description: status defines the observed state of AppBackup
            properties:
              backupStatus:
                description: BackupStatus captures the current status of a Velero
                  backup.
                properties:
                  backupItemOperationsAttempted:
                    description: |-
                      BackupItemOperationsAttempted is the total number of attempted
                      async BackupItemAction operations for this backup.
                    type: integer
                  backupItemOperationsCompleted:
                    description: |-
                      BackupItemOperationsCompleted is the total number of successfully completed
                      async BackupItemAction operations for this backup.
                    type: integer
                  backupItemOperationsFailed:
                    description: |-
                      BackupItemOperationsFailed is the total number of async
                      BackupItemAction operations for this backup which ended with an error.
                    type: integer
                  completionTimestamp:
                    description: |-
                      CompletionTimestamp records the time a backup was completed.
                      Completion time is recorded even on failed backups.
                      Completion time is recorded before uploading the backup object.
                      The server's time is used for CompletionTimestamps
                    format: date-time
                    nullable: true
                    type: string
                  csiVolumeSnapshotsAttempted:
                    description: |-
                      CSIVolumeSnapshotsAttempted is the total number of attempted
                      CSI VolumeSnapshots for this backup.
                    type: integer
                  csiVolumeSnapshotsCompleted:
                    description: |-
                      CSIVolumeSnapshotsCompleted is the total number of successfully
                      completed CSI VolumeSnapshots for this backup.
                    type: integer
                  errors:
                    description: |-
                      Errors is a count of all error messages that were generated during
                      execution of the backup.  The actual errors are in the backup's log
                      file in object storage.
                    type: integer
                  expiration:
                    description: Expiration is when this Backup is eligible for garbage-collection.
                    format: date-time
                    nullable: true
                    type: string
                  failureReason:
                    description: FailureReason is an error that caused the entire
                      backup to fail.
                    type: string
                  formatVersion:
                    description: FormatVersion is the backup format version, including
                      major, minor, and patch version.
                    type: string
                  hookStatus:
                    description: HookStatus contains information about the status
                      of the hooks.
                    nullable: true
                    properties:
                      hooksAttempted:
                        description: |-
                          HooksAttempted is the total number of attempted hooks
                          Specifically, HooksAttempted represents the number of hooks that failed to execute
                          and the number of hooks that executed successfully.
                        type: integer
                      hooksFailed:
                        description: HooksFailed is the total number of hooks which
                          ended with an error
                        type: integer
                    type: object
                  phase:
                    description: Phase is the current state of the Backup.
                    enum:
                    - New
                    - FailedValidation
                    - InProgress
                    - WaitingForPluginOperations
                    - WaitingForPluginOperationsPartiallyFailed
                    - Finalizing
                    - FinalizingPartiallyFailed
                    - Completed
                    - PartiallyFailed
                    - Failed
                    - Deleting
                    type: string
                  progress:
                    description: |-
                      Progress contains information about the backup's execution progress. Note
                      that this information is best-effort only -- if Velero fails to update it
                      during a backup for any reason, it may be inaccurate/stale.
                    nullable: true
                    properties:
                      itemsBackedUp:
                        description: |-
                          ItemsBackedUp is the number of items that have actually been written to the
                          backup tarball so far.
                        type: integer
                      totalItems:
                        description: |-
                          TotalItems is the total number of items to be backed up. This number may change
                          throughout the execution of the backup due to plugins that return additional related
                          items to back up, the velero.io/exclude-from-backup label, and various other
                          filters that happen as items are processed.
                        type: integer
                    type: object
                  startTimestamp:
                    description: |-
                      StartTimestamp records the time a backup was started.
                      Separate from CreationTimestamp, since that value changes
                      on restores.
                      The server's time is used for StartTimestamps
                    format: date-time
                    nullable: true
                    type: string
                  validationErrors:
                    description: |-
                      ValidationErrors is a slice of all validation errors (if
                      applicable).
                    items:
                      type: string
                    nullable: true
                    type: array
                  version:
                    description: |-
                      Version is the backup format major version.
                      Deprecated: Please see FormatVersion
                    type: integer
                  volumeSnapshotsAttempted:
                    description: |-
                      VolumeSnapshotsAttempted is the total number of attempted
                      volume snapshots for this backup.
                    type: integer
                  volumeSnapshotsCompleted:
                    description: |-
                      VolumeSnapshotsCompleted is the total number of successfully
                      completed volume snapshots for this backup.
                    type: integer
                  warnings:
                    description: |-
                      Warnings is a count of all warning messages that were generated during
                      execution of the backup. The actual warnings are in the backup's log
                      file in object storage.
                    type: integer
                type: object
              hasRunInitialBackup:
                description: HasRunInitialBackup records whether the initial one-off
                  backup has been run
                type: boolean
              history:
                description: History records the list of recent backups
                items:
                  properties:
                    completionTimestamp:
                      description: CompletionTimestamp is the time when the backup
                        completed
                      format: date-time
                      type: string
                    errors:
                      description: Errors is the number of errors that occurred during
                        the backup
                      type: integer
                    expiration:
                      description: Expiration is the time when the backup expires
                      format: date-time
                      type: string
                    managedStatus:
                      description: |-
                        ManagedStatus records the high-level status managed by the controller
                        Valid values: InProgress, Completed, Failed, Canceled
                      type: string
                    name:
                      description: Name is the name of the Velero Backup
                      type: string
                    phase:
                      description: Phase is the current phase of the Backup
                      type: string
                    startTimestamp:
                      description: StartTimestamp is the time when the backup was
                        started
                      format: date-time
                      type: string
                    veleroStatus:
                      description: VeleroStatus records the complete status from Velero
                        Backup
                      properties:
                        backupItemOperationsAttempted:
                          description: |-
                            BackupItemOperationsAttempted is the total number of attempted
                            async BackupItemAction operations for this backup.
                          type: integer
                        backupItemOperationsCompleted:
                          description: |-
                            BackupItemOperationsCompleted is the total number of successfully completed
                            async BackupItemAction operations for this backup.
                          type: integer
                        backupItemOperationsFailed:
                          description: |-
                            BackupItemOperationsFailed is the total number of async
                            BackupItemAction operations for this backup which ended with an error.
                          type: integer
                        completionTimestamp:
                          description: |-
                            CompletionTimestamp records the time a backup was completed.
                            Completion time is recorded even on failed backups.
                            Completion time is recorded before uploading the backup object.
                            The server's time is used for CompletionTimestamps
                          format: date-time
                          nullable: true
                          type: string
                        csiVolumeSnapshotsAttempted:
                          description: |-
                            CSIVolumeSnapshotsAttempted is the total number of attempted
                            CSI VolumeSnapshots for this backup.
                          type: integer
                        csiVolumeSnapshotsCompleted:
                          description: |-
                            CSIVolumeSnapshotsCompleted is the total number of successfully
                            completed CSI VolumeSnapshots for this backup.
                          type: integer
                        errors:
                          description: |-
                            Errors is a count of all error messages that were generated during
                            execution of the backup.  The actual errors are in the backup's log
                            file in object storage.
                          type: integer
                        expiration:
                          description: Expiration is when this Backup is eligible
                            for garbage-collection.
                          format: date-time
                          nullable: true
                          type: string
                        failureReason:
                          description: FailureReason is an error that caused the entire
                            backup to fail.
                          type: string
                        formatVersion:
                          description: FormatVersion is the backup format version,
                            including major, minor, and patch version.
                          type: string
                        hookStatus:
                          description: HookStatus contains information about the status
                            of the hooks.
                          nullable: true
                          properties:
                            hooksAttempted:
                              description: |-
                                HooksAttempted is the total number of attempted hooks
                                Specifically, HooksAttempted represents the number of hooks that failed to execute
                                and the number of hooks that executed successfully.
                              type: integer
                            hooksFailed:
                              description: HooksFailed is the total number of hooks
                                which ended with an error
                              type: integer
                          type: object
                        phase:
                          description: Phase is the current state of the Backup.
                          enum:
                          - New
                          - FailedValidation
                          - InProgress
                          - WaitingForPluginOperations
                          - WaitingForPluginOperationsPartiallyFailed
                          - Finalizing
                          - FinalizingPartiallyFailed
                          - Completed
                          - PartiallyFailed
                          - Failed
                          - Deleting
                          type: string
                        progress:
                          description: |-
                            Progress contains information about the backup's execution progress. Note
                            that this information is best-effort only -- if Velero fails to update it
                            during a backup for any reason, it may be inaccurate/stale.
                          nullable: true
                          properties:
                            itemsBackedUp:
                              description: |-
                                ItemsBackedUp is the number of items that have actually been written to the
                                backup tarball so far.
                              type: integer
                            totalItems:
                              description: |-
                                TotalItems is the total number of items to be backed up. This number may change
                                throughout the execution of the backup due to plugins that return additional related
                                items to back up, the velero.io/exclude-from-backup label, and various other
                                filters that happen as items are processed.
                              type: integer
                          type: object
                        startTimestamp:
                          description: |-
                            StartTimestamp records the time a backup was started.
                            Separate from CreationTimestamp, since that value changes
                            on restores.
                            The server's time is used for StartTimestamps
                          format: date-time
                          nullable: true
                          type: string
                        validationErrors:
                          description: |-
                            ValidationErrors is a slice of all validation errors (if
                            applicable).
                          items:
                            type: string
                          nullable: true
                          type: array
                        version:
                          description: |-
                            Version is the backup format major version.
                            Deprecated: Please see FormatVersion
                          type: integer
                        volumeSnapshotsAttempted:
                          description: |-
                            VolumeSnapshotsAttempted is the total number of attempted
                            volume snapshots for this backup.
                          type: integer
                        volumeSnapshotsCompleted:
                          description: |-
                            VolumeSnapshotsCompleted is the total number of successfully
                            completed volume snapshots for this backup.
                          type: integer
                        warnings:
                          description: |-
                            Warnings is a count of all warning messages that were generated during
                            execution of the backup. The actual warnings are in the backup's log
                            file in object storage.
                          type: integer
                      type: object
                    warnings:
                      description: Warnings is the number of warnings that occurred
                        during the backup
                      type: integer
                  required:
                  - name
                  - phase
                  type: object
                type: array
              lastAction:
                description: LastAction records the last processed manual action.
                properties:
                  requestAt:
                    description: |-
                      RequestAt is the time when the action was requested.
                      The controller will process the action if this timestamp is newer than the one in status.
                    format: date-time
                    type: string
                  targetBackup:
                    description: |-
                      TargetBackup specifies the name of the backup to operate on.
                      If empty, the action applies to the latest backup (for Retry/Cancel).
                      For Delete action, this field is required.
                    type: string
                  type:
                    description: |-
                      Type is the type of the action.
                      Valid values are: "Backup", "Retry", "Cancel", "Delete".
                    type: string
                required:
                - requestAt
                - type
                type: object
              latestBackupStatus:
                description: LastBackupStatus records the status of the last backup
                  (e.g., InProgress, Completed, Failed, Canceled)
                type: string
              message:
                description: Message provides a detailed message for the current status
                type: string
              reason:
                description: Reason indicates the reason for the current status
                type: string
              scheduleStatus:
                description: ScheduleStatus captures the current state of a Velero
                  schedule
                properties:
                  lastBackup:
                    description: |-
                      LastBackup is the last time a Backup was run for this
                      Schedule schedule
                    format: date-time
                    nullable: true
                    type: string
                  lastSkipped:
                    description: LastSkipped is the last time a Schedule was skipped
                    format: date-time
                    nullable: true
                    type: string
                  phase:
                    description: Phase is the current phase of the Schedule
                    enum:
                    - New
                    - Enabled
                    - FailedValidation
                    type: string
                  validationErrors:
                    description: |-
                      ValidationErrors is a slice of all validation errors (if
                      applicable)
                    items:
                      type: string
                    type: array
                type: object
              status:
                type: string
              totalBackups:
                description: TotalBackups records the total number of backups managed
                  by this AppBackup
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: apprestores.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: AppRestore
    listKind: AppRestoreList
    plural: apprestores
    singular: apprestore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The target cluster for the AppRestore
      jsonPath: .spec.cluster
      name: Cluster
      type: string
    - description: The source backup for the AppRestore
      jsonPath: .spec.backupSource
      name: BackupSource
      type: string
    - description: The status of the AppRestore
      jsonPath: .status.status
      name: Status
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: AppRestore is the Schema for the apprestores API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AppRestore
            properties:
              action:
                description: Action defines the manual action to be performed.
                properties:
                  requestAt:
                    description: RequestAt is the time when the action was requested.
                    format: date-time
                    type: string
                  type:
                    description: |-
                      Type is the type of the action.
                      Valid values are: "Restore", "Retry".
                    type: string
                required:
                - requestAt
                - type
                type: object
              backupSource:
                description: BackupSource specifies the source of the backup to restore
                type: string
              cluster:
                description: Cluster specifies the target cluster for the restore
                type: string
              resourceModifierRules:
                description: |-
                  ResourceModifierRules defines the rules for resource modification during restore.
                  If provided, the controller will generate a ConfigMap in the Velero namespace based on this content.
                items:
                  description: ResourceModifierRule defines a rule for modifying resources
                    during restore.
                  properties:
                    conditions:
                      description: Conditions specifies the conditions that must be
                        met for the patches to be applied.
                      properties:
                        groupResource:
                          description: GroupResource is the group and resource to
                            match (e.g., "persistentvolumeclaims" or "deployments.apps").
                          type: string
                        labelSelector:
                          description: LabelSelector is a label selector to match
                            resources.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        namespaces:
                          description: Namespaces is a list of namespaces to match.
                          items:
                            type: string
                          type: array
                        resourceNameRegex:
                          description: ResourceNameRegex is a regular expression to
                            match the resource name.
                          type: string
                      required:
                      - groupResource
                      type: object
                    patches:
                      description: Patches specifies the JSON patches to apply to
                        the matching resources.
                      items:
                        description: JSONPatch defines a JSON patch operation.
                        properties:
                          from:
                            description: From is the source path (for "move" or "copy"
                              operations).
                            type: string
                          operation:
                            description: Operation is the type of JSON patch operation
                              (e.g., "replace", "add", "remove").
                            type: string
                          path:
                            description: Path is the JSON path to the field to modify.
                            type: string
                          value:
                            description: Value is the value to set (for "replace"
                              or "add" operations).
                            type: string
                        required:
                        - operation
                        - path
                        type: object
                      type: array
                  required:
                  - conditions
                  - patches
                  type: object
                type: array
              sourceCluster:
                description: |-
                  SourceCluster specifies the source cluster of the backup.
                  If empty, defaults to the current cluster (same-cluster restore).
                  Required for cross-cluster restore.
                type: string
              storageRepository:
                description: |-
                  StorageRepository specifies the storage repository name (e.g., storage-1).
                  Required for cross-cluster restore to construct BSL name and path.
                type: string
              template:
                description: Template contains the restore configuration
                properties:
                  backupName:
                    description: |-
                      BackupName is the unique name of the Velero backup to restore
                      from.
                    type: string
                  excludedNamespaces:
                    description: |-
                      ExcludedNamespaces contains a list of namespaces that are not
                      included in the restore.
                    items:
                      type: string
                    nullable: true
                    type: array
                  excludedResources:
                    description: |-
                      ExcludedResources is a slice of resource names that are not
                      included in the restore.
                    items:
                      type: string
                    nullable: true
                    type: array
                  existingResourcePolicy:
                    description: ExistingResourcePolicy specifies the restore behavior
                      for the Kubernetes resource to be restored
                    nullable: true
                    type: string
                  hooks:
                    description: Hooks represent custom behaviors that should be executed
                      during or post restore.
                    properties:
                      resources:
                        items:
                          description: |-
                            RestoreResourceHookSpec defines one or more RestoreResrouceHooks that should be executed based on
                            the rules defined for namespaces, resources, and label selector.
                          properties:
                            excludedNamespaces:
                              description: ExcludedNamespaces specifies the namespaces
                                to which this hook spec does not apply.
                              items:
                                type: string
                              nullable: true
                              type: array
                            excludedResources:
                              description: ExcludedResources specifies the resources
                                to which this hook spec does not apply.
                              items:
                                type: string
                              nullable: true
                              type: array
                            includedNamespaces:
                              description: |-
                                IncludedNamespaces specifies the namespaces to which this hook spec applies. If empty, it applies
                                to all namespaces.
                              items:
                                type: string
                              nullable: true
                              type: array
                            includedResources:
                              description: |-
                                IncludedResources specifies the resources to which this hook spec applies. If empty, it applies
                                to all resources.
                              items:
                                type: string
                              nullable: true
                              type: array
                            labelSelector:
                              description: LabelSelector, if specified, filters the
                                resources to which this hook spec applies.
                              nullable: true
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            name:
                              description: Name is the name of this hook.
                              type: string
                            postHooks:
                              description: PostHooks is a list of RestoreResourceHooks
                                to execute during and after restoring a resource.
                              items:
                                description: RestoreResourceHook defines a restore
                                  hook for a resource.
                                properties:
                                  exec:
                                    description: Exec defines an exec restore hook.
                                    properties:
                                      command:
                                        description: Command is the command and arguments
                                          to execute from within a container after
                                          a pod has been restored.
                                        items:
                                          type: string
                                        minItems: 1
                                        type: array
                                      container:
                                        description: |-
                                          Container is the container in the pod where the command should be executed. If not specified,
                                          the pod's first container is used.
                                        type: string
                                      execTimeout:
                                        description: |-
                                          ExecTimeout defines the maximum amount of time Velero should wait for the hook to complete before
                                          considering the execution a failure.
                                        type: string
                                      onError:
                                        description: OnError specifies how Velero
                                          should behave if it encounters an error
                                          executing this hook.
                                        enum:
                                        - Continue
                                        - Fail
                                        type: string
                                      waitForReady:
                                        description: WaitForReady ensures command
                                          will be launched when container is Ready
                                          instead of Running.
                                        nullable: true
                                        type: boolean
                                      waitTimeout:
                                        description: |-
                                          WaitTimeout defines the maximum amount of time Velero should wait for the container to be Ready
                                          before attempting to run the command.
                                        type: string
                                    required:
                                    - command
                                    type: object
                                  init:
                                    description: Init defines an init restore hook.
                                    properties:
                                      initContainers:
                                        description: InitContainers is list of init
                                          containers to be added to a pod during its
                                          restore.
                                        items:
                                          type: object
                                          x-kubernetes-preserve-unknown-fields: true
                                        type: array
                                        x-kubernetes-preserve-unknown-fields: true
                                      timeout:
                                        description: Timeout defines the maximum amount
                                          of time Velero should wait for the initContainers
                                          to complete.
                                        type: string
                                    type: object
                                type: object
                              type: array
                          required:
                          - name
                          type: object
                        type: array
                    type: object
                  includeClusterResources:
                    description: |-
                      IncludeClusterResources specifies whether cluster-scoped resources
                      should be included for consideration in the restore. If null, defaults
                      to true.
                    nullable: true
                    type: boolean
                  includedNamespaces:
                    description: |-
                      IncludedNamespaces is a slice of namespace names to include objects
                      from. If empty, all namespaces are included.
                    items:
                      type: string
                    nullable: true
                    type: array
                  includedResources:
                    description: |-
                      IncludedResources is a slice of resource names to include
                      in the restore. If empty, all resources in the backup are included.
                    items:
                      type: string
                    nullable: true
                    type: array
                  itemOperationTimeout:
                    description: |-
                      ItemOperationTimeout specifies the time used to wait for RestoreItemAction operations
                      The default value is 4 hour.
                    type: string
                  labelSelector:
                    description: |-
                      LabelSelector is a metav1.LabelSelector to filter with
                      when restoring individual objects from the backup. If empty
                      or nil, all objects are included. Optional.
                    nullable: true
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  namespaceMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      NamespaceMapping is a map of source namespace names
                      to target namespace names to restore into. Any source
                      namespaces not included in the map will be restored into
                      namespaces of the same name.
                    type: object
                  orLabelSelectors:
                    description: |-
                      OrLabelSelectors is list of metav1.LabelSelector to filter with
                      when restoring individual objects from the backup. If multiple provided
                      they will be joined by the OR operator. LabelSelector as well as
                      OrLabelSelectors cannot co-exist in restore request, only one of them
                      can be used
                    items:
                      description: |-
                        A label selector is a label query over a set of resources. The result of matchLabels and
                        matchExpressions are ANDed. An empty label selector matches all objects. A null
                        label selector matches no objects.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    nullable: true
                    type: array
                  preserveNodePorts:
                    description: PreserveNodePorts specifies whether to restore old
                      nodePorts from backup.
                    nullable: true
                    type: boolean
                  resourceModifier:
                    description: ResourceModifier specifies the reference to JSON
                      resource patches that should be applied to resources before
                      restoration.
                    nullable: true
                    properties:
                      apiGroup:
                        description: |-
                          APIGroup is the group for the resource being referenced.
                          If APIGroup is not specified, the specified Kind must be in the core API group.
                          For any other third-party types, APIGroup is required.
                        type: string
                      kind:
                        description: Kind is the type of resource being referenced
                        type: string
                      name:
                        description: Name is the name of resource being referenced
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                    x-kubernetes-map-type: atomic
                  restorePVs:
                    description: |-
                      RestorePVs specifies whether to restore all included
                      PVs from snapshot
                    nullable: true
                    type: boolean
                  restoreStatus:
                    description: |-
                      RestoreStatus specifies which resources we should restore the status
                      field. If nil, no objects are included. Optional.
                    nullable: true
                    properties:
                      excludedResources:
                        description: ExcludedResources specifies the resources to
                          which will not restore the status.
                        items:
                          type: string
                        nullable: true
                        type: array
                      includedResources:
                        description: |-
                          IncludedResources specifies the resources to which will restore the status.
                          If empty, it applies to all resources.
                        items:
                          type: string
                        nullable: true
                        type: array
                    type: object
                  scheduleName:
                    description: |-
                      ScheduleName is the unique name of the Velero schedule to restore
                      from. If specified, and BackupName is empty, Velero will restore
                      from the most recent successful backup created from this schedule.
                    type: string
                  uploaderConfig:
                    description: UploaderConfig specifies the configuration for the
                      restore.
                    nullable: true
                    properties:
                      parallelFilesDownload:
                        description: ParallelFilesDownload is the concurrency number
                          setting for restore.
                        type: integer
                      writeSparseFiles:
                        description: WriteSparseFiles is a flag to indicate whether
                          write files sparsely or not.
                        nullable: true
                        type: boolean
                    type: object
                type: object
              timeout:
                description: |-
                  Timeout defines the maximum duration to wait for the restore to complete.
                  If exceeded, the controller will attempt to cancel/delete the Velero Restore.
                type: string
            required:
            - backupSource
            - cluster
            - template
            type: object
          status:
            description: status defines the observed state of AppRestore
            properties:
              lastAction:
                description: LastAction records the last processed manual action.
                properties:
                  requestAt:
                    description: RequestAt is the time when the action was requested.
                    format: date-time
                    type: string
                  type:
                    description: |-
                      Type is the type of the action.
                      Valid values are: "Restore", "Retry".
                    type: string
                required:
                - requestAt
                - type
                type: object
              message:
                description: Message provides a detailed message for the current status
                type: string
              reason:
                description: Reason indicates the reason for the current status
                type: string
              restoreStatus:
                description: RestoreStatus captures the current status of a Velero
                  restore
                properties:
                  completionTimestamp:
                    description: |-
                      CompletionTimestamp records the time the restore operation was completed.
                      Completion time is recorded even on failed restore.
                      The server's time is used for StartTimestamps
                    format: date-time
                    nullable: true
                    type: string
                  errors:
                    description: |-
                      Errors is a count of all error messages that were generated during
                      execution of the restore. The actual errors are stored in object storage.
                    type: integer
                  failureReason:
                    description: FailureReason is an error that caused the entire
                      restore to fail.
                    type: string
                  hookStatus:
                    description: HookStatus contains information about the status
                      of the hooks.
                    nullable: true
                    properties:
                      hooksAttempted:
                        description: |-
                          HooksAttempted is the total number of attempted hooks
                          Specifically, HooksAttempted represents the number of hooks that failed to execute
                          and the number of hooks that executed successfully.
                        type: integer
                      hooksFailed:
                        description: HooksFailed is the total number of hooks which
                          ended with an error
                        type: integer
                    type: object
                  phase:
                    description: Phase is the current state of the Restore
                    enum:
                    - New
                    - FailedValidation
                    - InProgress
                    - WaitingForPluginOperations
                    - WaitingForPluginOperationsPartiallyFailed
                    - Completed
                    - PartiallyFailed
                    - Failed
                    - Finalizing
                    - FinalizingPartiallyFailed
                    type: string
                  progress:
                    description: |-
                      Progress contains information about the restore's execution progress. Note
                      that this information is best-effort only -- if Velero fails to update it
                      during a restore for any reason, it may be inaccurate/stale.
                    nullable: true
                    properties:
                      itemsRestored:
                        description: ItemsRestored is the number of items that have
                          actually been restored so far
                        type: integer
                      totalItems:
                        description: |-
                          TotalItems is the total number of items to be restored. This number may change
                          throughout the execution of the restore due to plugins that return additional related
                          items to restore
                        type: integer
                    type: object
                  restoreItemOperationsAttempted:
                    description: |-
                      RestoreItemOperationsAttempted is the total number of attempted
                      async RestoreItemAction operations for this restore.
                    type: integer
                  restoreItemOperationsCompleted:
                    description: |-
                      RestoreItemOperationsCompleted is the total number of successfully completed
                      async RestoreItemAction operations for this restore.
                    type: integer
                  restoreItemOperationsFailed:
                    description: |-
                      RestoreItemOperationsFailed is the total number of async
                      RestoreItemAction operations for this restore which ended with an error.
                    type: integer
                  startTimestamp:
                    description: |-
                      StartTimestamp records the time the restore operation was started.
                      The server's time is used for StartTimestamps
                    format: date-time
                    nullable: true
                    type: string
                  validationErrors:
                    description: |-
                      ValidationErrors is a slice of all validation errors (if
                      applicable)
                    items:
                      type: string
                    nullable: true
                    type: array
                  warnings:
                    description: |-
                      Warnings is a count of all warning messages that were generated during
                      execution of the restore. The actual warnings are stored in object storage.
                    type: integer
                type: object
              status:
                description: AppRestorePhase defines the phase of an AppRestore
                enum:
                - Pending
                - Restoring
                - InProgress
                - Succeeded
                - Failed
                - Cancelled
                - Initiating
                - Deleting
                - Unknown
                type: string
              targetNamespaces:
                description: TargetNamespaces records the list of namespaces that
                  will be restored
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: backuppolicies.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: BackupPolicy
    listKind: BackupPolicyList
    plural: backuppolicies
    singular: backuppolicy
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: BackupPolicy is the Schema for the backuppolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of BackupPolicy
            properties:
              schedule:
                type: string
              startTime:
                format: date-time
                type: string
              ttl:
                type: string
            required:
            - schedule
            type: object
          status:
            description: status defines the observed state of BackupPolicy
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: backuprestorestatisticses.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: BackupRestoreStatistics
    listKind: BackupRestoreStatisticsList
    plural: backuprestorestatisticses
    singular: backuprestorestatistics
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.scopeType
      name: ScopeType
      type: string
    - jsonPath: .status.statistics.total
      name: Total
      type: integer
    - jsonPath: .status.statistics.inProgress
      name: InProgress
      type: integer
    - jsonPath: .status.statistics.completed
      name: Completed
      type: integer
    - jsonPath: .status.statistics.failed
      name: Failed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: BackupRestoreStatistics is the Schema for the backuprestorestatistics
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupRestoreStatisticsSpec defines the desired state of
              BackupRestoreStatistics
            properties:
              scopeRef:
                description: ScopeRef defines the reference to the scope object
                properties:
                  apiVersion:
                    description: APIVersion of the referent.
                    type: string
                  kind:
                    description: Kind of the referent.
                    type: string
                  name:
                    description: Name of the referent.
                    type: string
                  namespace:
                    description: Namespace of the referent.
                    type: string
                  uid:
                    description: UID of the referent.
                    type: string
                type: object
              scopeType:
                description: ScopeType defines the scope of the statistics
                enum:
                - app
                - namespace
                - cluster
                - custom
                type: string
            required:
            - scopeRef
            - scopeType
            type: object
          status:
            description: BackupRestoreStatisticsStatus defines the observed state
              of BackupRestoreStatistics
            properties:
              associatedResources:
                description: AssociatedResources is a list of resources associated
                  with the statistics
                items:
                  description: AssociatedResource defines a resource associated with
                    the statistics
                  properties:
                    apiVersion:
                      description: APIVersion of the referent.
                      type: string
                    kind:
                      description: Kind of the referent.
                      type: string
                    name:
                      description: Name of the referent.
                      type: string
                    namespace:
                      description: Namespace of the referent.
                      type: string
                    role:
                      description: Role of the resource (e.g., "backup", "restore")
                      type: string
                    uid:
                      description: UID of the referent.
                      type: string
                  type: object
                type: array
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              eventLog:
                description: EventLog is a log of recent events (capped at 100)
                items:
                  description: StatisticsEvent defines an event in the statistics
                    history
                  properties:
                    changes:
                      additionalProperties:
                        format: int32
                        type: integer
                      description: Changes applied in this event
                      type: object
                    message:
                      description: Message describing the event
                      type: string
                    reason:
                      description: Reason for the event
                      type: string
                    timestamp:
                      description: Timestamp of the event
                      format: date-time
                      type: string
                    type:
                      description: Type of the event (e.g., "Increment", "Decrement",
                        "Transition", "Sync")
                      type: string
                  required:
                  - timestamp
                  - type
                  type: object
                type: array
              lastUpdateReason:
                description: LastUpdateReason is the reason for the last update
                type: string
              lastUpdateTime:
                description: LastUpdateTime is the last time the statistics were updated
                format: date-time
                type: string
              statistics:
                description: Statistics contains the counters
                properties:
                  canceled:
                    default: 0
                    description: Canceled number of operations
                    format: int32
                    type: integer
                  completed:
                    default: 0
                    description: Completed number of operations
                    format: int32
                    type: integer
                  failed:
                    default: 0
                    description: Failed number of operations
                    format: int32
                    type: integer
                  inProgress:
                    default: 0
                    description: InProgress number of operations
                    format: int32
                    type: integer
                  total:
                    default: 0
                    description: Total number of operations
                    format: int32
                    type: integer
                  unknown:
                    default: 0
                    description: Unknown number of operations
                    format: int32
                    type: integer
                required:
                - canceled
                - completed
                - failed
                - inProgress
                - total
                - unknown
                type: object
            required:
            - statistics
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: clusters.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    shortNames:
    - cluster
    singular: cluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Cluster phase
      jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: Velero version
      jsonPath: .status.veleroVersion
      name: Velero
      type: string
    - description: Cluster version
      jsonPath: .status.k8sVersion
      name: Version
      type: string
    - description: Cluster Serever
      jsonPath: .status.endpoint
      name: Endpoint
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Cluster is the Schema for the clusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Cluster
            properties:
              endpoint:
                type: string
              imageSources:
                description: ImageSources defines reusable registry aliases for this
                  cluster.
                items:
                  description: |-
                    ImageSource defines an available image registry source on a cluster.
                    It is referenced by DisasterInstance image rewrite mappings.
                  properties:
                    name:
                      description: Name is a readable alias and must be unique within
                        one Cluster.
                      type: string
                    registry:
                      description: Registry is the registry host/prefix, e.g. harbor.prod.local.
                      type: string
                  required:
                  - name
                  - registry
                  type: object
                type: array
              kubeConfig:
                format: byte
                type: string
              token:
                type: string
              veleroInstall:
                description: VeleroInstall defines Velero install-specific image registry
                  settings.
                properties:
                  imageRegistry:
                    description: |-
                      ImageRegistry is the registry/repository prefix used for Velero install images.
                      Example: harbor.customer.local/disaster
                    type: string
                  registryCredentialSecretRef:
                    description: RegistryCredentialSecretRef references the management-plane
                      dockerconfigjson Secret.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
            type: object
          status:
            description: status defines the observed state of Cluster
            properties:
              endpoint:
                type: string
              k8sVersion:
                type: string
              lastCheckTime:
                description: LastCheckTime records the time when the stats were last
                  collected
                format: date-time
                type: string
              lastEventPhase:
                description: LastEventPhase records the last event phase emitted,
                  used to avoid duplicate events
                type: string
              message:
                type: string
              namespaceCount:
                description: NamespaceCount is the number of non-system namespaces
                  tracked by the cluster backup statistics.
                type: integer
              namespaceStats:
                additionalProperties:
                  type: integer
                description: NamespaceStats records the backup-scope resource count
                  per tracked namespace.
                type: object
              nodeCount:
                type: integer
              observedGeneration:
                format: int64
                type: integer
              observedMetadataHash:
                description: ObservedMetadataHash records the hash of the metadata
                  (labels/annotations) that was last processed
                type: string
              readyTimestamp:
                description: ReadyTimestamp records when the cluster became Ready,
                  used for Duration calculation
                format: date-time
                type: string
              reason:
                type: string
              resourceTotalCount:
                description: ResourceTotalCount is the total number of namespace-scoped
                  backup-scope resources across tracked namespaces.
                type: integer
              status:
                type: string
              tokenExpiration:
                description: TokenExpiration records the expiration time of the token
                  if it is a JWT
                format: date-time
                type: string
              veleroVersion:
                type: string
              workloadNamespaceCount:
                description: WorkloadNamespaceCount is the number of namespaces that
                  contain running Deployment/StatefulSet workloads
                type: integer
              workloadNamespaceStats:
                additionalProperties:
                  type: integer
                description: WorkloadNamespaceStats records the backup-scope resource
                  count per tracked namespace for namespaces that contain running
                  Deployment/StatefulSet workloads.
                type: object
              workloadTotalCount:
                description: WorkloadTotalCount is the total number of namespace-scoped
                  backup-scope resources across tracked namespaces that contain running
                  Deployment/StatefulSet workloads.
                type: integer
            required:
            - endpoint
            - k8sVersion
            - status
            - veleroVersion
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: datasyncs.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: DataSync
    listKind: DataSyncList
    plural: datasyncs
    shortNames:
    - ds
    singular: datasync
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: 父级 DisasterInstance
      jsonPath: .spec.instance
      name: Instance
      type: string
    - description: 当前状态
      jsonPath: .status.state
      name: State
      type: string
    - description: 上次同步时间
      jsonPath: .status.lastSyncTime
      name: LastSync
      type: date
    - description: 是否暂停
      jsonPath: .spec.paused
      name: Paused
      type: boolean
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          DataSync 是数据同步的 API Schema
          使用 Trafficless Restore 方案管理 PVC 数据同步
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataSyncSpec 定义 DataSync 的期望状态
            properties:
              instance:
                description: Instance 父级 DisasterInstance 的名称
                type: string
              paused:
                description: Paused 指示是否暂停同步
                type: boolean
              trafficlessConfig:
                description: TrafficlessConfig 定义 Trafficless Restore 的设置
                properties:
                  command:
                    description: 'Command 隐形 Pod 的启动命令（默认: ["sleep", "3600"]）'
                    items:
                      type: string
                    type: array
                  image:
                    default: busybox:latest
                    description: 'Image 隐形 Pod 使用的容器镜像（默认: busybox:latest）'
                    type: string
                  removeLabels:
                    description: RemoveLabels 需要移除的标签列表，防止 Pod 接收服务流量
                    items:
                      type: string
                    type: array
                type: object
              trigger:
                description: Trigger 定义同步的触发方式
                properties:
                  manual:
                    description: |-
                      Manual 手动触发时间戳
                      设置为当前时间以触发立即同步
                    type: string
                  schedule:
                    description: |-
                      Schedule 定时调度表达式（从 DisasterPolicy 继承）
                      只读字段，由引用的策略填充
                    type: string
                type: object
            required:
            - instance
            type: object
          status:
            description: DataSyncStatus 定义 DataSync 的观测状态
            properties:
              conditions:
                description: Conditions 最新的观测状态
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              history:
                description: History 记录最近的同步历史
                items:
                  description: SyncHistoryRecord 定义单次同步的历史记录
                  properties:
                    backupName:
                      description: BackupName 关联的 AppBackup 名称
                      type: string
                    backupResourceCount:
                      description: BackupResourceCount 备份资源数
                      type: integer
                    completionTime:
                      description: CompletionTime 同步结束时间
                      format: date-time
                      type: string
                    duration:
                      description: Duration 同步耗时
                      type: string
                    restoreName:
                      description: RestoreName 关联的 AppRestore 名称 (如果有)
                      type: string
                    restoreResourceCount:
                      description: RestoreResourceCount 恢复资源数
                      type: integer
                    startTime:
                      description: StartTime 同步开始时间
                      format: date-time
                      type: string
                    status:
                      description: Status 同步结果状态 (Completed/Failed)
                      type: string
                  type: object
                type: array
              lastBackupName:
                description: LastBackupName 上次创建的 AppBackup 名称
                type: string
              lastRestoreName:
                description: LastRestoreName 上次创建的 AppRestore 名称
                type: string
              lastSyncTime:
                description: LastSyncTime 上次同步时间
                format: date-time
                type: string
              message:
                description: Message 人类可读错误描述
                type: string
              reason:
                description: Reason 机器可读错误码
                type: string
              state:
                description: State 当前同步状态
                type: string
              trafficlessPods:
                description: TrafficlessPods 当前隐形 Pod 列表
                items:
                  description: TrafficlessPodStatus 表示隐形 Pod 的状态
                  properties:
                    name:
                      description: Name Pod 名称
                      type: string
                    namespace:
                      description: Namespace Pod 所在命名空间
                      type: string
                    phase:
                      description: Phase Pod 阶段（Running, Completed 等）
                      type: string
                    pvcName:
                      description: PVCName 该 Pod 挂载的 PVC 名称
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: disasterbackups.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: DisasterBackup
    listKind: DisasterBackupList
    plural: disasterbackups
    singular: disasterbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - description: DisasterBackup Phase
      jsonPath: .status.phase
      name: Phase
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: DisasterBackup is the Schema for the disasterbackups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DisasterBackup
            properties:
              disasterConfig:
                type: string
              labelSelector:
                description: |-
                  LabelSelector is a metav1.LabelSelector to filter with
                  when adding individual objects to the backup. If empty
                  or nil, all objects are included. Optional.
                nullable: true
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              namespace:
                type: string
              newNamespace:
                type: string
              newStorageName:
                type: string
            required:
            - disasterConfig
            type: object
          status:
            description: status defines the observed state of DisasterBackup
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the DisasterBackup resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                type: string
              resources:
                additionalProperties:
                  items:
                    properties:
                      apiVersion:
                        description: |-
                          APIVersion defines the versioned schema of this representation of an object.
                          Servers should convert recognized schemas to the latest internal value, and
                          may reject unrecognized values.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                        type: string
                      kind:
                        description: |-
                          Kind is a string value representing the REST resource this object represents.
                          Servers may infer this from the endpoint the client submits requests to.
                          Cannot be updated.
                          In CamelCase.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                        type: string
                      names:
                        items:
                          type: string
                        type: array
                    required:
                    - names
                    type: object
                  type: array
                type: object
              updateTime:
                format: date-time
                type: string
              workload:
                additionalProperties:
                  items:
                    properties:
                      apiVersion:
                        description: |-
                          APIVersion defines the versioned schema of this representation of an object.
                          Servers should convert recognized schemas to the latest internal value, and
                          may reject unrecognized values.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                        type: string
                      kind:
                        description: |-
                          Kind is a string value representing the REST resource this object represents.
                          Servers may infer this from the endpoint the client submits requests to.
                          Cannot be updated.
                          In CamelCase.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                        type: string
                      names:
                        items:
                          type: string
                        type: array
                    required:
                    - names
                    type: object
                  type: array
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: disasterconfigs.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: DisasterConfig
    listKind: DisasterConfigList
    plural: disasterconfigs
    shortNames:
    - dc
    singular: disasterconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: 灾备源集群名称
      jsonPath: .spec.sourceCluster
      name: SourceCluster
      type: string
    - description: 灾备目标集群名称
      jsonPath: .spec.targetCluster
      name: TargetCluster
      type: string
    - description: 灾备配置状态
      jsonPath: .status.status
      name: Status
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: DisasterConfig is the Schema for the disasterconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DisasterConfig
            properties:
              dataSyncInterval:
                description: 'Deprecated: Use DataSyncPolicy instead'
                type: integer
              dataSyncPolicy:
                description: |-
                  DataSyncPolicy is the name of the DisasterPolicy for data synchronization
                  The referenced policy must have type=DataSync
                type: string
              dataSyncType:
                default: fsb
                description: |-
                  DataSyncType specifies the data synchronization type
                  Options: "fsb" (File System Backup), "snapshot", "none", "external"
                type: string
              imageRewrite:
                description: ImageRewrite defines image source mapping strategy for
                  restore workflows.
                properties:
                  applyTo:
                    description: |-
                      ApplyTo controls which restore paths apply image rewrite.
                      Supported values: resourceSync, drill.
                    items:
                      description: ImageRewriteApplyTarget defines where image rewrite
                        rules should take effect.
                      type: string
                    type: array
                  enabled:
                    description: Enabled toggles image rewrite behavior.
                    type: boolean
                  mappings:
                    description: Mappings defines source image source alias -> target
                      image source alias rules.
                    items:
                      description: ImageSourceMapping defines source->target image
                        source alias mapping.
                      properties:
                        sourceImageSource:
                          description: SourceImageSource references Cluster.spec.imageSources[].name
                            on source cluster.
                          type: string
                        targetImageSource:
                          description: TargetImageSource references Cluster.spec.imageSources[].name
                            on target cluster.
                          type: string
                      required:
                      - sourceImageSource
                      - targetImageSource
                      type: object
                    type: array
                  unmatchedPolicy:
                    description: |-
                      UnmatchedPolicy controls behavior when image is not matched by any mapping.
                      Supported values: Fail, Keep. Default is Fail.
                    enum:
                    - Fail
                    - Keep
                    type: string
                type: object
              resourceSyncPolicy:
                description: |-
                  ResourceSyncPolicy is the name of the DisasterPolicy for resource synchronization
                  The referenced policy must have type=ResourceSync
                type: string
              resourcesSyncInterval:
                description: 'Deprecated: Use ResourceSyncPolicy instead'
                type: integer
              resourcesSyncPolicy:
                description: 'Deprecated: Not used in V2'
                type: integer
              sourceCluster:
                description: SourceCluster is the name of the source cluster (Cluster
                  CR)
                type: string
              storageRepository:
                description: StorageRepository is the name of the storage repository
                  (StorageRepository CR)
                type: string
              targetCluster:
                description: TargetCluster is the name of the target cluster (Cluster
                  CR)
                type: string
            required:
            - sourceCluster
            - storageRepository
            - targetCluster
            type: object
          status:
            description: status defines the observed state of DisasterConfig
            properties:
              message:
                type: string
              reason:
                type: string
              status:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: disasterdrills.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: DisasterDrill
    listKind: DisasterDrillList
    plural: disasterdrills
    shortNames:
    - drill
    - drills
    singular: disasterdrill
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.instanceName
      name: Instance
      type: string
    - jsonPath: .spec.groupName
      name: Group
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.restoreMode
      name: RestoreMode
      type: string
    - jsonPath: .status.targetCluster
      name: TargetCluster
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: DisasterDrill 容灾演练资源
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DisasterDrillSpec 定义演练规格
            properties:
              cleanup:
                description: CleanUp 触发演练资源清理
                type: boolean
              confirmed:
                description: Confirmed 用户确认开始执行 (Ready -> Executing)
                type: boolean
              groupName:
                description: GroupName 关联的容灾组名称 (与 InstanceName 二选一)
                type: string
              instanceName:
                description: InstanceName 关联的容灾实例名称 (与 GroupName 二选一)
                type: string
              namespaceMapping:
                additionalProperties:
                  type: string
                description: |-
                  NamespaceMapping 命名空间映射 (可选，不指定则使用原始命名空间)
                  格式: 源命名空间 -> 目标命名空间
                type: object
              restorePolicy:
                description: |-
                  RestorePolicy 演练级 restorePolicy 覆盖配置。
                  未提供时，默认继承 DisasterInstance 的 restorePolicy；
                  提供时，演练链路会优先使用该配置中的资源定制化/bulk 修改等策略。
                properties:
                  bulkModifierActions:
                    description: BulkModifierActions defines instance-level bulk modifier
                      intent.
                    items:
                      description: BulkModifierAction defines instance-level bulk
                        modifier input.
                      properties:
                        action:
                          description: |-
                            Action controls bulk expansion behavior.
                            Supported values: replaceExactValue, removeKey.
                          enum:
                          - replaceExactValue
                          - removeKey
                          type: string
                        applyTo:
                          description: ApplyTo scopes action to resourceSync/drill
                            restore paths.
                          items:
                            description: RestoreModifierApplyTarget defines which
                              restore path a rule applies to.
                            type: string
                          type: array
                        directionPolicy:
                          description: DirectionPolicy controls generated rule direction
                            semantics.
                          enum:
                          - Auto
                          - ForwardOnly
                          - ReverseOnly
                          type: string
                        enabled:
                          description: Enabled gates whether action participates in
                            snapshot generation. Default true.
                          type: boolean
                        id:
                          description: ID is optional user-facing action id for diagnostics
                            and deterministic rule IDs.
                          type: string
                        key:
                          description: Key is required when action=removeKey.
                          type: string
                        sourceValue:
                          description: SourceValue is required when action=replaceExactValue.
                          type: string
                        targetValue:
                          description: TargetValue is required when action=replaceExactValue.
                          type: string
                      required:
                      - action
                      type: object
                    type: array
                  execution:
                    description: Execution controls restore execution behavior.
                    properties:
                      existingResourcePolicy:
                        description: |-
                          ExistingResourcePolicy controls how to handle existing resources.
                          Supported values: none, update.
                        enum:
                        - none
                        - update
                        - None
                        - Update
                        type: string
                      itemOperationTimeout:
                        description: ItemOperationTimeout configures velero restore
                          item operation timeout.
                        type: string
                      restorePVs:
                        description: RestorePVs controls whether to restore PersistentVolumes.
                        type: boolean
                    type: object
                  ingressClassMapping:
                    description: IngressClassMapping defines IngressClass mapping
                      behavior.
                    properties:
                      mappings:
                        description: Mappings defines source->target mapping rules.
                        items:
                          description: RestoreClassMapping defines source->target
                            class mapping for restore.
                          properties:
                            namespaces:
                              description: Namespaces optionally scopes this mapping
                                to specific namespaces.
                              items:
                                type: string
                              type: array
                            sourceClass:
                              description: SourceClass is the class name in source
                                cluster.
                              type: string
                            targetClass:
                              description: TargetClass is the class name to use in
                                target cluster.
                              type: string
                          required:
                          - sourceClass
                          - targetClass
                          type: object
                        type: array
                      strictTargetValidation:
                        description: StrictTargetValidation checks whether target
                          classes exist before restore starts.
                        type: boolean
                      unmatchedPolicy:
                        description: |-
                          UnmatchedPolicy controls behavior when mapping is not matched.
                          Supported values: Keep, Fail. Default is Keep.
                        enum:
                        - Keep
                        - Fail
                        type: string
                    type: object
                  modifierRuleSnapshot:
                    description: ModifierRuleSnapshot defines server-generated final
                      executable modifier rule set.
                    items:
                      description: RestoreModifierRule defines unified modifier DSL
                        rule.
                      properties:
                        applyTo:
                          description: ApplyTo scopes rule to dataSync/resourceSync/drill
                            paths.
                          items:
                            description: RestoreModifierApplyTarget defines which
                              restore path a rule applies to.
                            type: string
                          type: array
                        conditions:
                          description: Conditions reuses Velero condition structure.
                          properties:
                            groupResource:
                              description: GroupResource is the group and resource
                                to match (e.g., "persistentvolumeclaims" or "deployments.apps").
                              type: string
                            labelSelector:
                              description: LabelSelector is a label selector to match
                                resources.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            namespaces:
                              description: Namespaces is a list of namespaces to match.
                              items:
                                type: string
                              type: array
                            resourceNameRegex:
                              description: ResourceNameRegex is a regular expression
                                to match the resource name.
                              type: string
                          required:
                          - groupResource
                          type: object
                        directionPolicy:
                          description: DirectionPolicy gates rule by resolved flow.
                            Default Auto.
                          enum:
                          - Auto
                          - ForwardOnly
                          - ReverseOnly
                          type: string
                        enabled:
                          description: Enabled gates whether rule participates in
                            compilation. Default true.
                          type: boolean
                        id:
                          description: ID is optional user-facing rule id for diagnostics.
                          type: string
                        mode:
                          description: |-
                            Mode controls compilation behavior.
                            Supported values: veleroNative, reversible.
                          enum:
                          - veleroNative
                          - reversible
                          type: string
                        onConflict:
                          description: OnConflict controls same-priority same-key
                            conflict handling. Default Fail.
                          enum:
                          - Fail
                          - Skip
                          type: string
                        pair:
                          description: |-
                            Pair is required when mode=reversible.
                            Pair values may contain restricted placeholders.
                          properties:
                            path:
                              description: Path is JSON Pointer target path.
                              minLength: 1
                              type: string
                            sourceValue:
                              description: |-
                                SourceValue is the value for the baseline source side.
                                The selected value may contain restricted placeholders.
                              minLength: 1
                              type: string
                            targetValue:
                              description: |-
                                TargetValue is the value for the baseline target side.
                                The selected value may contain restricted placeholders.
                              minLength: 1
                              type: string
                          required:
                          - path
                          - sourceValue
                          - targetValue
                          type: object
                        priority:
                          description: Priority decides conflict precedence, larger
                            wins.
                          format: int32
                          type: integer
                        veleroRule:
                          description: VeleroRule is required when mode=veleroNative.
                          properties:
                            mergePatches:
                              description: MergePatches is reserved for Phase 2.
                              items:
                                type: string
                              type: array
                            patches:
                              description: Patches is Velero JSONPatch list (Phase
                                1 supported).
                              items:
                                description: JSONPatch defines a JSON patch operation.
                                properties:
                                  from:
                                    description: From is the source path (for "move"
                                      or "copy" operations).
                                    type: string
                                  operation:
                                    description: Operation is the type of JSON patch
                                      operation (e.g., "replace", "add", "remove").
                                    type: string
                                  path:
                                    description: Path is the JSON path to the field
                                      to modify.
                                    type: string
                                  value:
                                    description: Value is the value to set (for "replace"
                                      or "add" operations).
                                    type: string
                                required:
                                - operation
                                - path
                                type: object
                              type: array
                            strategicPatches:
                              description: StrategicPatches is reserved for Phase
                                2.
                              items:
                                type: string
                              type: array
                          type: object
                      required:
                      - conditions
                      - mode
                      type: object
                    type: array
                  modifierRuleSnapshotHash:
                    description: ModifierRuleSnapshotHash is SHA256 over final modifierRuleSnapshot
                      JSON.
                    type: string
                  modifierRules:
                    description: ModifierRules defines unified reversible/veleroNative
                      rule set.
                    items:
                      description: RestoreModifierRule defines unified modifier DSL
                        rule.
                      properties:
                        applyTo:
                          description: ApplyTo scopes rule to dataSync/resourceSync/drill
                            paths.
                          items:
                            description: RestoreModifierApplyTarget defines which
                              restore path a rule applies to.
                            type: string
                          type: array
                        conditions:
                          description: Conditions reuses Velero condition structure.
                          properties:
                            groupResource:
                              description: GroupResource is the group and resource
                                to match (e.g., "persistentvolumeclaims" or "deployments.apps").
                              type: string
                            labelSelector:
                              description: LabelSelector is a label selector to match
                                resources.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            namespaces:
                              description: Namespaces is a list of namespaces to match.
                              items:
                                type: string
                              type: array
                            resourceNameRegex:
                              description: ResourceNameRegex is a regular expression
                                to match the resource name.
                              type: string
                          required:
                          - groupResource
                          type: object
                        directionPolicy:
                          description: DirectionPolicy gates rule by resolved flow.
                            Default Auto.
                          enum:
                          - Auto
                          - ForwardOnly
                          - ReverseOnly
                          type: string
                        enabled:
                          description: Enabled gates whether rule participates in
                            compilation. Default true.
                          type: boolean
                        id:
                          description: ID is optional user-facing rule id for diagnostics.
                          type: string
                        mode:
                          description: |-
                            Mode controls compilation behavior.
                            Supported values: veleroNative, reversible.
                          enum:
                          - veleroNative
                          - reversible
                          type: string
                        onConflict:
                          description: OnConflict controls same-priority same-key
                            conflict handling. Default Fail.
                          enum:
                          - Fail
                          - Skip
                          type: string
                        pair:
                          description: |-
                            Pair is required when mode=reversible.
                            Pair values may contain restricted placeholders.
                          properties:
                            path:
                              description: Path is JSON Pointer target path.
                              minLength: 1
                              type: string
                            sourceValue:
                              description: |-
                                SourceValue is the value for the baseline source side.
                                The selected value may contain restricted placeholders.
                              minLength: 1
                              type: string
                            targetValue:
                              description: |-
                                TargetValue is the value for the baseline target side.
                                The selected value may contain restricted placeholders.
                              minLength: 1
                              type: string
                          required:
                          - path
                          - sourceValue
                          - targetValue
                          type: object
                        priority:
                          description: Priority decides conflict precedence, larger
                            wins.
                          format: int32
                          type: integer
                        veleroRule:
                          description: VeleroRule is required when mode=veleroNative.
                          properties:
                            mergePatches:
                              description: MergePatches is reserved for Phase 2.
                              items:
                                type: string
                              type: array
                            patches:
                              description: Patches is Velero JSONPatch list (Phase
                                1 supported).
                              items:
                                description: JSONPatch defines a JSON patch operation.
                                properties:
                                  from:
                                    description: From is the source path (for "move"
                                      or "copy" operations).
                                    type: string
                                  operation:
                                    description: Operation is the type of JSON patch
                                      operation (e.g., "replace", "add", "remove").
                                    type: string
                                  path:
                                    description: Path is the JSON path to the field
                                      to modify.
                                    type: string
                                  value:
                                    description: Value is the value to set (for "replace"
                                      or "add" operations).
                                    type: string
                                required:
                                - operation
                                - path
                                type: object
                              type: array
                            strategicPatches:
                              description: StrategicPatches is reserved for Phase
                                2.
                              items:
                                type: string
                              type: array
                          type: object
                      required:
                      - conditions
                      - mode
                      type: object
                    type: array
                  resourceSelection:
                    description: ResourceSelection controls restore include/exclude
                      scope.
                    properties:
                      excludedClusterScopedResources:
                        description: ExcludedClusterScopedResources specifies cluster-scoped
                          resources to exclude.
                        items:
                          type: string
                        type: array
                      excludedNamespaceScopedResources:
                        description: ExcludedNamespaceScopedResources specifies namespace-scoped
                          resources to exclude.
                        items:
                          type: string
                        type: array
                      excludedNamespaces:
                        description: ExcludedNamespaces specifies namespaces to exclude.
                        items:
                          type: string
                        type: array
                      excludedResources:
                        description: ExcludedResources specifies resources to exclude.
                        items:
                          type: string
                        type: array
                      includeClusterResources:
                        description: IncludeClusterResources controls whether cluster-scoped
                          resources are included.
                        type: boolean
                      includedClusterScopedResources:
                        description: IncludedClusterScopedResources specifies cluster-scoped
                          resources to include.
                        items:
                          type: string
                        type: array
                      includedNamespaceScopedResources:
                        description: IncludedNamespaceScopedResources specifies namespace-scoped
                          resources to include.
                        items:
                          type: string
                        type: array
                      includedNamespaces:
                        description: IncludedNamespaces specifies namespaces to include.
                        items:
                          type: string
                        type: array
                      includedResources:
                        description: IncludedResources specifies resources to include.
                        items:
                          type: string
                        type: array
                      labelSelector:
                        description: LabelSelector filters resources by labels.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  storageClassMapping:
                    description: StorageClassMapping defines StorageClass mapping
                      behavior.
                    properties:
                      mappings:
                        description: Mappings defines source->target mapping rules.
                        items:
                          description: RestoreClassMapping defines source->target
                            class mapping for restore.
                          properties:
                            namespaces:
                              description: Namespaces optionally scopes this mapping
                                to specific namespaces.
                              items:
                                type: string
                              type: array
                            sourceClass:
                              description: SourceClass is the class name in source
                                cluster.
                              type: string
                            targetClass:
                              description: TargetClass is the class name to use in
                                target cluster.
                              type: string
                          required:
                          - sourceClass
                          - targetClass
                          type: object
                        type: array
                      strictTargetValidation:
                        description: StrictTargetValidation checks whether target
                          classes exist before restore starts.
                        type: boolean
                      unmatchedPolicy:
                        description: |-
                          UnmatchedPolicy controls behavior when mapping is not matched.
                          Supported values: Keep, Fail. Default is Keep.
                        enum:
                        - Keep
                        - Fail
                        type: string
                    type: object
                  useUnifiedDirectionResolver:
                    description: |-
                      UseUnifiedDirectionResolver toggles unified direction resolver + DSL compiler.
                      Default false in Phase 1 for smooth migration.
                    type: boolean
                type: object
              skipValidation:
                description: SkipValidation 跳过前置校验
                type: boolean
              targetCluster:
                description: TargetCluster 演练目标集群 (可选，不指定则使用 Instance 的 secondaryCluster)
                type: string
              waitUntilReady:
                description: WaitUntilReady 是否等待 Pod 就绪
                type: boolean
            type: object
          status:
            description: DisasterDrillStatus 定义演练状态
            properties:
              completionTime:
                description: CompletionTime 完成时间
                format: date-time
                type: string
              currentStep:
                description: CurrentStep 当前执行步骤
                type: string
              executionTime:
                description: ExecutionTime 用户确认执行时间
                format: date-time
                type: string
              groupProgress:
                description: GroupProgress 容灾组演练进度
                properties:
                  currentLevelIndex:
                    description: CurrentLevelIndex 当前执行的层级 (0-based)
                    type: integer
                  levelStatuses:
                    description: LevelStatuses 各层详细状态
                    items:
                      description: LevelStatus 记录 Group 操作中每一层的执行状态
                      properties:
                        completionTime:
                          description: CompletionTime 完成时间
                          format: date-time
                          type: string
                        failedInstances:
                          description: FailedInstances 执行失败的实例
                          items:
                            type: string
                          type: array
                        index:
                          description: Index 层级索引 (0-based)
                          type: integer
                        instances:
                          description: Instances 该层包含的实例列表
                          items:
                            type: string
                          type: array
                        startTime:
                          description: StartTime 开始时间
                          format: date-time
                          type: string
                        state:
                          description: State 状态 (Pending, Running, Completed, Failed,
                            TimedOut)
                          type: string
                      required:
                      - index
                      - instances
                      - state
                      type: object
                    type: array
                  totalLevels:
                    description: TotalLevels 总层数
                    type: integer
                required:
                - currentLevelIndex
                - totalLevels
                type: object
              message:
                description: Message 状态消息
                type: string
              operationName:
                description: OperationName 关联的 DisasterOperation 名称
                type: string
              readyTime:
                description: ReadyTime 校验完成时间
                format: date-time
                type: string
              reason:
                description: Reason 机器可读错误码
                type: string
              restoreMode:
                description: RestoreMode 恢复模式
                enum:
                - Reuse
                - FullRestore
                type: string
              restoreName:
                description: RestoreName 演练创建的 AppRestore 名称 (仅完整恢复模式)
                type: string
              startTime:
                description: StartTime 创建时间
                format: date-time
                type: string
              state:
                description: State 当前状态
                enum:
                - Pending
                - Ready
                - Executing
                - Completed
                - CleaningUp
                - CleanedUp
                - Failed
                type: string
              steps:
                description: Steps 步骤详情
                items:
                  description: StepStatus 表示单个步骤的状态
                  properties:
                    completionTime:
                      description: CompletionTime 步骤完成时间
                      format: date-time
                      type: string
                    message:
                      description: Message 附加信息
                      type: string
                    name:
                      description: Name 步骤名称
                      type: string
                    startTime:
                      description: StartTime 步骤开始时间
                      format: date-time
                      type: string
                    state:
                      description: State 步骤状态 (Pending/Running/Completed/Failed/Skipped)
                      type: string
                  required:
                  - name
                  - state
                  type: object
                type: array
              targetCluster:
                description: TargetCluster 实际使用的目标集群
                type: string
              validationResults:
                description: ValidationResults 校验结果
                properties:
                  backupAvailable:
                    description: BackupAvailable 备份是否可用
                    type: boolean
                  clusterReachable:
                    description: ClusterReachable 目标集群是否可达
                    type: boolean
                  instanceValid:
                    description: InstanceValid Instance 状态是否有效
                    type: boolean
                  lastDataSyncTime:
                    description: LastDataSyncTime 最近数据同步时间
                    format: date-time
                    type: string
                  lastResourceSyncTime:
                    description: LastResourceSyncTime 最近资源同步时间
                    format: date-time
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: disastergroups.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: DisasterGroup
    listKind: DisasterGroupList
    plural: disastergroups
    shortNames:
    - dg
    singular: disastergroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: 实例总数
      jsonPath: .status.totalInstances
      name: Total
      type: integer
    - description: 就绪实例数
      jsonPath: .status.readyInstances
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          DisasterGroup 是容灾组的 API Schema
          它定义了一组 DisasterInstance 及其切换顺序
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DisasterGroupSpec 定义 DisasterGroup 的期望状态
            properties:
              levels:
                description: |-
                  Levels 定义切换顺序，二维数组。
                  Index 0 (Level 1) 最先执行。同一 Level 内并行执行。
                items:
                  items:
                    type: string
                  type: array
                type: array
              policy:
                description: Policy 策略配置
                properties:
                  failPolicy:
                    description: 'FailPolicy 失败处理策略: "Continue" (忽略错误继续执行) / "Stop"
                      (停止执行)'
                    type: string
                  parallelism:
                    description: Parallelism 全局最大并行实例数。0 表示无限制。
                    type: integer
                  retryPolicy:
                    description: RetryPolicy 默认重试策略
                    properties:
                      maxRetries:
                        default: 0
                        description: MaxRetries 最大重试次数，默认为 0（不重试）
                        format: int32
                        type: integer
                      retryIntervalSeconds:
                        default: 5
                        description: RetryIntervalSeconds 重试间隔秒数，默认为 5
                        format: int32
                        type: integer
                    type: object
                  timeoutMin:
                    description: TimeoutMin 单个 Level 的超时时间(分钟)。超过则根据 FailPolicy 处理。
                    type: integer
                type: object
            required:
            - levels
            type: object
          status:
            description: DisasterGroupStatus 定义 DisasterGroup 的观测状态
            properties:
              conditions:
                description: Conditions 聚合状态 (如 GroupReady, GroupFailingOver)
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message 人类可读错误描述
                type: string
              readyInstances:
                description: ReadyInstances 处于 Protected 状态的实例数
                type: integer
              reason:
                description: Reason 机器可读错误码
                type: string
              totalInstances:
                description: TotalInstances 组内实例总数
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: disasterinstances.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: DisasterInstance
    listKind: DisasterInstanceList
    plural: disasterinstances
    shortNames:
    - di
    singular: disasterinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: 关联的 DisasterConfig
      jsonPath: .spec.config
      name: Config
      type: string
    - description: 当前状态机状态
      jsonPath: .status.fsmState
      name: State
      type: string
    - description: 主集群
      jsonPath: .status.primaryCluster
      name: Primary
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          DisasterInstance 是容灾实例的 API Schema
          它表示两个集群之间针对一组工作负载的容灾关系
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DisasterInstanceSpec 定义 DisasterInstance 的期望状态
            properties:
              config:
                description: Config 关联的 DisasterConfig 名称
                type: string
              dataSyncPolicy:
                description: |-
                  DataSyncPolicy 定义实例级数据同步策略 override。
                  为空时继承关联 DisasterConfig.spec.dataSyncPolicy。
                type: string
              labelSelector:
                description: LabelSelector 用于选择需要保护的资源
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              namespaces:
                description: Namespaces 需要保护的命名空间列表
                items:
                  type: string
                type: array
              operationTimeoutMinutes:
                default: 60
                description: |-
                  OperationTimeoutMinutes 定义该实例执行容灾操作的默认超时时间(分钟)
                  默认值: 60 (1小时)
                format: int32
                type: integer
              podRestoreMethod:
                default: replica
                description: |-
                  PodRestoreMethod 指定 Standby 模式下的 Pod 处理方式
                  可选值: "replica" (默认), "initContainer"
                type: string
              resourceSyncPolicy:
                description: |-
                  ResourceSyncPolicy 定义实例级资源同步策略 override。
                  为空时继承关联 DisasterConfig.spec.resourceSyncPolicy。
                type: string
              restorePolicy:
                description: |-
                  RestorePolicy 定义实例级恢复策略。
                  DataSync/ResourceSync/Drill 在构建 AppRestore 时会应用该策略。
                properties:
                  bulkModifierActions:
                    description: BulkModifierActions defines instance-level bulk modifier
                      intent.
                    items:
                      description: BulkModifierAction defines instance-level bulk
                        modifier input.
                      properties:
                        action:
                          description: |-
                            Action controls bulk expansion behavior.
                            Supported values: replaceExactValue, removeKey.
                          enum:
                          - replaceExactValue
                          - removeKey
                          type: string
                        applyTo:
                          description: ApplyTo scopes action to resourceSync/drill
                            restore paths.
                          items:
                            description: RestoreModifierApplyTarget defines which
                              restore path a rule applies to.
                            type: string
                          type: array
                        directionPolicy:
                          description: DirectionPolicy controls generated rule direction
                            semantics.
                          enum:
                          - Auto
                          - ForwardOnly
                          - ReverseOnly
                          type: string
                        enabled:
                          description: Enabled gates whether action participates in
                            snapshot generation. Default true.
                          type: boolean
                        id:
                          description: ID is optional user-facing action id for diagnostics
                            and deterministic rule IDs.
                          type: string
                        key:
                          description: Key is required when action=removeKey.
                          type: string
                        sourceValue:
                          description: SourceValue is required when action=replaceExactValue.
                          type: string
                        targetValue:
                          description: TargetValue is required when action=replaceExactValue.
                          type: string
                      required:
                      - action
                      type: object
                    type: array
                  execution:
                    description: Execution controls restore execution behavior.
                    properties:
                      existingResourcePolicy:
                        description: |-
                          ExistingResourcePolicy controls how to handle existing resources.
                          Supported values: none, update.
                        enum:
                        - none
                        - update
                        - None
                        - Update
                        type: string
                      itemOperationTimeout:
                        description: ItemOperationTimeout configures velero restore
                          item operation timeout.
                        type: string
                      restorePVs:
                        description: RestorePVs controls whether to restore PersistentVolumes.
                        type: boolean
                    type: object
                  ingressClassMapping:
                    description: IngressClassMapping defines IngressClass mapping
                      behavior.
                    properties:
                      mappings:
                        description: Mappings defines source->target mapping rules.
                        items:
                          description: RestoreClassMapping defines source->target
                            class mapping for restore.
                          properties:
                            namespaces:
                              description: Namespaces optionally scopes this mapping
                                to specific namespaces.
                              items:
                                type: string
                              type: array
                            sourceClass:
                              description: SourceClass is the class name in source
                                cluster.
                              type: string
                            targetClass:
                              description: TargetClass is the class name to use in
                                target cluster.
                              type: string
                          required:
                          - sourceClass
                          - targetClass
                          type: object
                        type: array
                      strictTargetValidation:
                        description: StrictTargetValidation checks whether target
                          classes exist before restore starts.
                        type: boolean
                      unmatchedPolicy:
                        description: |-
                          UnmatchedPolicy controls behavior when mapping is not matched.
                          Supported values: Keep, Fail. Default is Keep.
                        enum:
                        - Keep
                        - Fail
                        type: string
                    type: object
                  modifierRuleSnapshot:
                    description: ModifierRuleSnapshot defines server-generated final
                      executable modifier rule set.
                    items:
                      description: RestoreModifierRule defines unified modifier DSL
                        rule.
                      properties:
                        applyTo:
                          description: ApplyTo scopes rule to dataSync/resourceSync/drill
                            paths.
                          items:
                            description: RestoreModifierApplyTarget defines which
                              restore path a rule applies to.
                            type: string
                          type: array
                        conditions:
                          description: Conditions reuses Velero condition structure.
                          properties:
                            groupResource:
                              description: GroupResource is the group and resource
                                to match (e.g., "persistentvolumeclaims" or "deployments.apps").
                              type: string
                            labelSelector:
                              description: LabelSelector is a label selector to match
                                resources.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            namespaces:
                              description: Namespaces is a list of namespaces to match.
                              items:
                                type: string
                              type: array
                            resourceNameRegex:
                              description: ResourceNameRegex is a regular expression
                                to match the resource name.
                              type: string
                          required:
                          - groupResource
                          type: object
                        directionPolicy:
                          description: DirectionPolicy gates rule by resolved flow.
                            Default Auto.
                          enum:
                          - Auto
                          - ForwardOnly
                          - ReverseOnly
                          type: string
                        enabled:
                          description: Enabled gates whether rule participates in
                            compilation. Default true.
                          type: boolean
                        id:
                          description: ID is optional user-facing rule id for diagnostics.
                          type: string
                        mode:
                          description: |-
                            Mode controls compilation behavior.
                            Supported values: veleroNative, reversible.
                          enum:
                          - veleroNative
                          - reversible
                          type: string
                        onConflict:
                          description: OnConflict controls same-priority same-key
                            conflict handling. Default Fail.
                          enum:
                          - Fail
                          - Skip
                          type: string
                        pair:
                          description: |-
                            Pair is required when mode=reversible.
                            Pair values may contain restricted placeholders.
                          properties:
                            path:
                              description: Path is JSON Pointer target path.
                              minLength: 1
                              type: string
                            sourceValue:
                              description: |-
                                SourceValue is the value for the baseline source side.
                                The selected value may contain restricted placeholders.
                              minLength: 1
                              type: string
                            targetValue:
                              description: |-
                                TargetValue is the value for the baseline target side.
                                The selected value may contain restricted placeholders.
                              minLength: 1
                              type: string
                          required:
                          - path
                          - sourceValue
                          - targetValue
                          type: object
                        priority:
                          description: Priority decides conflict precedence, larger
                            wins.
                          format: int32
                          type: integer
                        veleroRule:
                          description: VeleroRule is required when mode=veleroNative.
                          properties:
                            mergePatches:
                              description: MergePatches is reserved for Phase 2.
                              items:
                                type: string
                              type: array
                            patches:
                              description: Patches is Velero JSONPatch list (Phase
                                1 supported).
                              items:
                                description: JSONPatch defines a JSON patch operation.
                                properties:
                                  from:
                                    description: From is the source path (for "move"
                                      or "copy" operations).
                                    type: string
                                  operation:
                                    description: Operation is the type of JSON patch
                                      operation (e.g., "replace", "add", "remove").
                                    type: string
                                  path:
                                    description: Path is the JSON path to the field
                                      to modify.
                                    type: string
                                  value:
                                    description: Value is the value to set (for "replace"
                                      or "add" operations).
                                    type: string
                                required:
                                - operation
                                - path
                                type: object
                              type: array
                            strategicPatches:
                              description: StrategicPatches is reserved for Phase
                                2.
                              items:
                                type: string
                              type: array
                          type: object
                      required:
                      - conditions
                      - mode
                      type: object
                    type: array
                  modifierRuleSnapshotHash:
                    description: ModifierRuleSnapshotHash is SHA256 over final modifierRuleSnapshot
                      JSON.
                    type: string
                  modifierRules:
                    description: ModifierRules defines unified reversible/veleroNative
                      rule set.
                    items:
                      description: RestoreModifierRule defines unified modifier DSL
                        rule.
                      properties:
                        applyTo:
                          description: ApplyTo scopes rule to dataSync/resourceSync/drill
                            paths.
                          items:
                            description: RestoreModifierApplyTarget defines which
                              restore path a rule applies to.
                            type: string
                          type: array
                        conditions:
                          description: Conditions reuses Velero condition structure.
                          properties:
                            groupResource:
                              description: GroupResource is the group and resource
                                to match (e.g., "persistentvolumeclaims" or "deployments.apps").
                              type: string
                            labelSelector:
                              description: LabelSelector is a label selector to match
                                resources.
                              properties:
                                matchExpressions:
                                  description: matchExpressions is a list of label
                                    selector requirements. The requirements are ANDed.
                                  items:
                                    description: |-
                                      A label selector requirement is a selector that contains values, a key, and an operator that
                                      relates the key and values.
                                    properties:
                                      key:
                                        description: key is the label key that the
                                          selector applies to.
                                        type: string
                                      operator:
                                        description: |-
                                          operator represents a key's relationship to a set of values.
                                          Valid operators are In, NotIn, Exists and DoesNotExist.
                                        type: string
                                      values:
                                        description: |-
                                          values is an array of string values. If the operator is In or NotIn,
                                          the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                          the values array must be empty. This array is replaced during a strategic
                                          merge patch.
                                        items:
                                          type: string
                                        type: array
                                        x-kubernetes-list-type: atomic
                                    required:
                                    - key
                                    - operator
                                    type: object
                                  type: array
                                  x-kubernetes-list-type: atomic
                                matchLabels:
                                  additionalProperties:
                                    type: string
                                  description: |-
                                    matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                    map is equivalent to an element of matchExpressions, whose key field is "key", the
                                    operator is "In", and the values array contains only "value". The requirements are ANDed.
                                  type: object
                              type: object
                              x-kubernetes-map-type: atomic
                            namespaces:
                              description: Namespaces is a list of namespaces to match.
                              items:
                                type: string
                              type: array
                            resourceNameRegex:
                              description: ResourceNameRegex is a regular expression
                                to match the resource name.
                              type: string
                          required:
                          - groupResource
                          type: object
                        directionPolicy:
                          description: DirectionPolicy gates rule by resolved flow.
                            Default Auto.
                          enum:
                          - Auto
                          - ForwardOnly
                          - ReverseOnly
                          type: string
                        enabled:
                          description: Enabled gates whether rule participates in
                            compilation. Default true.
                          type: boolean
                        id:
                          description: ID is optional user-facing rule id for diagnostics.
                          type: string
                        mode:
                          description: |-
                            Mode controls compilation behavior.
                            Supported values: veleroNative, reversible.
                          enum:
                          - veleroNative
                          - reversible
                          type: string
                        onConflict:
                          description: OnConflict controls same-priority same-key
                            conflict handling. Default Fail.
                          enum:
                          - Fail
                          - Skip
                          type: string
                        pair:
                          description: |-
                            Pair is required when mode=reversible.
                            Pair values may contain restricted placeholders.
                          properties:
                            path:
                              description: Path is JSON Pointer target path.
                              minLength: 1
                              type: string
                            sourceValue:
                              description: |-
                                SourceValue is the value for the baseline source side.
                                The selected value may contain restricted placeholders.
                              minLength: 1
                              type: string
                            targetValue:
                              description: |-
                                TargetValue is the value for the baseline target side.
                                The selected value may contain restricted placeholders.
                              minLength: 1
                              type: string
                          required:
                          - path
                          - sourceValue
                          - targetValue
                          type: object
                        priority:
                          description: Priority decides conflict precedence, larger
                            wins.
                          format: int32
                          type: integer
                        veleroRule:
                          description: VeleroRule is required when mode=veleroNative.
                          properties:
                            mergePatches:
                              description: MergePatches is reserved for Phase 2.
                              items:
                                type: string
                              type: array
                            patches:
                              description: Patches is Velero JSONPatch list (Phase
                                1 supported).
                              items:
                                description: JSONPatch defines a JSON patch operation.
                                properties:
                                  from:
                                    description: From is the source path (for "move"
                                      or "copy" operations).
                                    type: string
                                  operation:
                                    description: Operation is the type of JSON patch
                                      operation (e.g., "replace", "add", "remove").
                                    type: string
                                  path:
                                    description: Path is the JSON path to the field
                                      to modify.
                                    type: string
                                  value:
                                    description: Value is the value to set (for "replace"
                                      or "add" operations).
                                    type: string
                                required:
                                - operation
                                - path
                                type: object
                              type: array
                            strategicPatches:
                              description: StrategicPatches is reserved for Phase
                                2.
                              items:
                                type: string
                              type: array
                          type: object
                      required:
                      - conditions
                      - mode
                      type: object
                    type: array
                  resourceSelection:
                    description: ResourceSelection controls restore include/exclude
                      scope.
                    properties:
                      excludedClusterScopedResources:
                        description: ExcludedClusterScopedResources specifies cluster-scoped
                          resources to exclude.
                        items:
                          type: string
                        type: array
                      excludedNamespaceScopedResources:
                        description: ExcludedNamespaceScopedResources specifies namespace-scoped
                          resources to exclude.
                        items:
                          type: string
                        type: array
                      excludedNamespaces:
                        description: ExcludedNamespaces specifies namespaces to exclude.
                        items:
                          type: string
                        type: array
                      excludedResources:
                        description: ExcludedResources specifies resources to exclude.
                        items:
                          type: string
                        type: array
                      includeClusterResources:
                        description: IncludeClusterResources controls whether cluster-scoped
                          resources are included.
                        type: boolean
                      includedClusterScopedResources:
                        description: IncludedClusterScopedResources specifies cluster-scoped
                          resources to include.
                        items:
                          type: string
                        type: array
                      includedNamespaceScopedResources:
                        description: IncludedNamespaceScopedResources specifies namespace-scoped
                          resources to include.
                        items:
                          type: string
                        type: array
                      includedNamespaces:
                        description: IncludedNamespaces specifies namespaces to include.
                        items:
                          type: string
                        type: array
                      includedResources:
                        description: IncludedResources specifies resources to include.
                        items:
                          type: string
                        type: array
                      labelSelector:
                        description: LabelSelector filters resources by labels.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                  storageClassMapping:
                    description: StorageClassMapping defines StorageClass mapping
                      behavior.
                    properties:
                      mappings:
                        description: Mappings defines source->target mapping rules.
                        items:
                          description: RestoreClassMapping defines source->target
                            class mapping for restore.
                          properties:
                            namespaces:
                              description: Namespaces optionally scopes this mapping
                                to specific namespaces.
                              items:
                                type: string
                              type: array
                            sourceClass:
                              description: SourceClass is the class name in source
                                cluster.
                              type: string
                            targetClass:
                              description: TargetClass is the class name to use in
                                target cluster.
                              type: string
                          required:
                          - sourceClass
                          - targetClass
                          type: object
                        type: array
                      strictTargetValidation:
                        description: StrictTargetValidation checks whether target
                          classes exist before restore starts.
                        type: boolean
                      unmatchedPolicy:
                        description: |-
                          UnmatchedPolicy controls behavior when mapping is not matched.
                          Supported values: Keep, Fail. Default is Keep.
                        enum:
                        - Keep
                        - Fail
                        type: string
                    type: object
                  useUnifiedDirectionResolver:
                    description: |-
                      UseUnifiedDirectionResolver toggles unified direction resolver + DSL compiler.
                      Default false in Phase 1 for smooth migration.
                    type: boolean
                type: object
              skipPodReadyCheck:
                description: |-
                  SkipPodReadyCheck 定义该实例执行容灾切换时是否默认跳过 Pod 就绪校验。
                  true: 默认跳过容器就绪验证（仅校验副本配置已下发）
                  false: 默认执行容器就绪验证（检查 readyReplicas）
                  该默认值可被 DisasterOperation 入参覆盖。
                type: boolean
            required:
            - config
            type: object
          status:
            description: DisasterInstanceStatus 定义 DisasterInstance 的观测状态
            properties:
              availableOperations:
                description: AvailableOperations 当前状态下可执行的操作列表
                items:
                  type: string
                type: array
              conditions:
                description: Conditions 实例状态的最新观测
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dataSyncName:
                description: DataSyncName 管理的 DataSync 资源名称
                type: string
              fsmState:
                description: FsmState 当前状态机状态
                type: string
              lastDataSyncTime:
                description: LastDataSyncTime 上次数据同步时间
                format: date-time
                type: string
              lastResourceSyncTime:
                description: LastResourceSyncTime 上次资源同步时间
                format: date-time
                type: string
              lastStableFsmState:
                description: LastStableFsmState 进入 ConfigError 前的原始状态，用于配置恢复时回放
                type: string
              message:
                description: Message 人类可读错误描述
                type: string
              primaryCluster:
                description: PrimaryCluster 当前主集群（活跃集群）名称
                type: string
              reason:
                description: Reason 机器可读错误码
                type: string
              resourceSyncName:
                description: ResourceSyncName 管理的 ResourceSync 资源名称
                type: string
              secondaryCluster:
                description: SecondaryCluster 当前备集群（待命集群）名称
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: disasterjobs.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: DisasterJob
    listKind: DisasterJobList
    plural: disasterjobs
    singular: disasterjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: 状态
      jsonPath: .status.phase
      name: Phase
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: DisasterJob is the Schema for the disasterjobs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DisasterJob
            properties:
              disasterBackup:
                type: string
              scheduleType:
                type: string
              syncType:
                type: string
            required:
            - disasterBackup
            type: object
          status:
            description: status defines the observed state of DisasterJob
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the DisasterJob resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                type: string
              reason:
                type: string
              startTime:
                format: date-time
                type: string
            required:
            - phase
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: disasteroperations.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: DisasterOperation
    listKind: DisasterOperationList
    plural: disasteroperations
    shortNames:
    - do
    singular: disasteroperation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: 目标 DisasterInstance
      jsonPath: .spec.instanceName
      name: Instance
      type: string
    - description: 目标 DisasterGroup
      jsonPath: .spec.groupName
      name: Group
      type: string
    - description: 操作类型
      jsonPath: .spec.operationType
      name: Type
      type: string
    - description: 操作状态
      jsonPath: .status.state
      name: State
      type: string
    - description: 当前步骤
      jsonPath: .status.currentStep
      name: CurrentStep
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          DisasterOperation 是容灾操作的 API Schema
          表示一次性操作，如故障切换、故障回切、暂停或恢复
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DisasterOperationSpec 定义 DisasterOperation 的期望状态
            properties:
              directive:
                description: Directive 运行时指令
                properties:
                  confirmed:
                    description: Confirmed 用户确认执行 (用于 drill 等需要二次确认的操作)
                    type: boolean
                type: object
              drillConfig:
                description: DrillConfig 演练配置 (仅当 operationType=drill 时有效)
                properties:
                  namespaceMapping:
                    additionalProperties:
                      type: string
                    description: |-
                      NamespaceMapping 命名空间映射 (可选)
                      格式: 源命名空间 -> 目标命名空间
                    type: object
                  restorePolicy:
                    description: |-
                      RestorePolicy 演练级 restorePolicy 覆盖配置。
                      仅 drill/drill-cleanup 相关编排使用，未提供时继承实例 restorePolicy。
                    properties:
                      bulkModifierActions:
                        description: BulkModifierActions defines instance-level bulk
                          modifier intent.
                        items:
                          description: BulkModifierAction defines instance-level bulk
                            modifier input.
                          properties:
                            action:
                              description: |-
                                Action controls bulk expansion behavior.
                                Supported values: replaceExactValue, removeKey.
                              enum:
                              - replaceExactValue
                              - removeKey
                              type: string
                            applyTo:
                              description: ApplyTo scopes action to resourceSync/drill
                                restore paths.
                              items:
                                description: RestoreModifierApplyTarget defines which
                                  restore path a rule applies to.
                                type: string
                              type: array
                            directionPolicy:
                              description: DirectionPolicy controls generated rule
                                direction semantics.
                              enum:
                              - Auto
                              - ForwardOnly
                              - ReverseOnly
                              type: string
                            enabled:
                              description: Enabled gates whether action participates
                                in snapshot generation. Default true.
                              type: boolean
                            id:
                              description: ID is optional user-facing action id for
                                diagnostics and deterministic rule IDs.
                              type: string
                            key:
                              description: Key is required when action=removeKey.
                              type: string
                            sourceValue:
                              description: SourceValue is required when action=replaceExactValue.
                              type: string
                            targetValue:
                              description: TargetValue is required when action=replaceExactValue.
                              type: string
                          required:
                          - action
                          type: object
                        type: array
                      execution:
                        description: Execution controls restore execution behavior.
                        properties:
                          existingResourcePolicy:
                            description: |-
                              ExistingResourcePolicy controls how to handle existing resources.
                              Supported values: none, update.
                            enum:
                            - none
                            - update
                            - None
                            - Update
                            type: string
                          itemOperationTimeout:
                            description: ItemOperationTimeout configures velero restore
                              item operation timeout.
                            type: string
                          restorePVs:
                            description: RestorePVs controls whether to restore PersistentVolumes.
                            type: boolean
                        type: object
                      ingressClassMapping:
                        description: IngressClassMapping defines IngressClass mapping
                          behavior.
                        properties:
                          mappings:
                            description: Mappings defines source->target mapping rules.
                            items:
                              description: RestoreClassMapping defines source->target
                                class mapping for restore.
                              properties:
                                namespaces:
                                  description: Namespaces optionally scopes this mapping
                                    to specific namespaces.
                                  items:
                                    type: string
                                  type: array
                                sourceClass:
                                  description: SourceClass is the class name in source
                                    cluster.
                                  type: string
                                targetClass:
                                  description: TargetClass is the class name to use
                                    in target cluster.
                                  type: string
                              required:
                              - sourceClass
                              - targetClass
                              type: object
                            type: array
                          strictTargetValidation:
                            description: StrictTargetValidation checks whether target
                              classes exist before restore starts.
                            type: boolean
                          unmatchedPolicy:
                            description: |-
                              UnmatchedPolicy controls behavior when mapping is not matched.
                              Supported values: Keep, Fail. Default is Keep.
                            enum:
                            - Keep
                            - Fail
                            type: string
                        type: object
                      modifierRuleSnapshot:
                        description: ModifierRuleSnapshot defines server-generated
                          final executable modifier rule set.
                        items:
                          description: RestoreModifierRule defines unified modifier
                            DSL rule.
                          properties:
                            applyTo:
                              description: ApplyTo scopes rule to dataSync/resourceSync/drill
                                paths.
                              items:
                                description: RestoreModifierApplyTarget defines which
                                  restore path a rule applies to.
                                type: string
                              type: array
                            conditions:
                              description: Conditions reuses Velero condition structure.
                              properties:
                                groupResource:
                                  description: GroupResource is the group and resource
                                    to match (e.g., "persistentvolumeclaims" or "deployments.apps").
                                  type: string
                                labelSelector:
                                  description: LabelSelector is a label selector to
                                    match resources.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: Namespaces is a list of namespaces
                                    to match.
                                  items:
                                    type: string
                                  type: array
                                resourceNameRegex:
                                  description: ResourceNameRegex is a regular expression
                                    to match the resource name.
                                  type: string
                              required:
                              - groupResource
                              type: object
                            directionPolicy:
                              description: DirectionPolicy gates rule by resolved
                                flow. Default Auto.
                              enum:
                              - Auto
                              - ForwardOnly
                              - ReverseOnly
                              type: string
                            enabled:
                              description: Enabled gates whether rule participates
                                in compilation. Default true.
                              type: boolean
                            id:
                              description: ID is optional user-facing rule id for
                                diagnostics.
                              type: string
                            mode:
                              description: |-
                                Mode controls compilation behavior.
                                Supported values: veleroNative, reversible.
                              enum:
                              - veleroNative
                              - reversible
                              type: string
                            onConflict:
                              description: OnConflict controls same-priority same-key
                                conflict handling. Default Fail.
                              enum:
                              - Fail
                              - Skip
                              type: string
                            pair:
                              description: |-
                                Pair is required when mode=reversible.
                                Pair values may contain restricted placeholders.
                              properties:
                                path:
                                  description: Path is JSON Pointer target path.
                                  minLength: 1
                                  type: string
                                sourceValue:
                                  description: |-
                                    SourceValue is the value for the baseline source side.
                                    The selected value may contain restricted placeholders.
                                  minLength: 1
                                  type: string
                                targetValue:
                                  description: |-
                                    TargetValue is the value for the baseline target side.
                                    The selected value may contain restricted placeholders.
                                  minLength: 1
                                  type: string
                              required:
                              - path
                              - sourceValue
                              - targetValue
                              type: object
                            priority:
                              description: Priority decides conflict precedence, larger
                                wins.
                              format: int32
                              type: integer
                            veleroRule:
                              description: VeleroRule is required when mode=veleroNative.
                              properties:
                                mergePatches:
                                  description: MergePatches is reserved for Phase
                                    2.
                                  items:
                                    type: string
                                  type: array
                                patches:
                                  description: Patches is Velero JSONPatch list (Phase
                                    1 supported).
                                  items:
                                    description: JSONPatch defines a JSON patch operation.
                                    properties:
                                      from:
                                        description: From is the source path (for
                                          "move" or "copy" operations).
                                        type: string
                                      operation:
                                        description: Operation is the type of JSON
                                          patch operation (e.g., "replace", "add",
                                          "remove").
                                        type: string
                                      path:
                                        description: Path is the JSON path to the
                                          field to modify.
                                        type: string
                                      value:
                                        description: Value is the value to set (for
                                          "replace" or "add" operations).
                                        type: string
                                    required:
                                    - operation
                                    - path
                                    type: object
                                  type: array
                                strategicPatches:
                                  description: StrategicPatches is reserved for Phase
                                    2.
                                  items:
                                    type: string
                                  type: array
                              type: object
                          required:
                          - conditions
                          - mode
                          type: object
                        type: array
                      modifierRuleSnapshotHash:
                        description: ModifierRuleSnapshotHash is SHA256 over final
                          modifierRuleSnapshot JSON.
                        type: string
                      modifierRules:
                        description: ModifierRules defines unified reversible/veleroNative
                          rule set.
                        items:
                          description: RestoreModifierRule defines unified modifier
                            DSL rule.
                          properties:
                            applyTo:
                              description: ApplyTo scopes rule to dataSync/resourceSync/drill
                                paths.
                              items:
                                description: RestoreModifierApplyTarget defines which
                                  restore path a rule applies to.
                                type: string
                              type: array
                            conditions:
                              description: Conditions reuses Velero condition structure.
                              properties:
                                groupResource:
                                  description: GroupResource is the group and resource
                                    to match (e.g., "persistentvolumeclaims" or "deployments.apps").
                                  type: string
                                labelSelector:
                                  description: LabelSelector is a label selector to
                                    match resources.
                                  properties:
                                    matchExpressions:
                                      description: matchExpressions is a list of label
                                        selector requirements. The requirements are
                                        ANDed.
                                      items:
                                        description: |-
                                          A label selector requirement is a selector that contains values, a key, and an operator that
                                          relates the key and values.
                                        properties:
                                          key:
                                            description: key is the label key that
                                              the selector applies to.
                                            type: string
                                          operator:
                                            description: |-
                                              operator represents a key's relationship to a set of values.
                                              Valid operators are In, NotIn, Exists and DoesNotExist.
                                            type: string
                                          values:
                                            description: |-
                                              values is an array of string values. If the operator is In or NotIn,
                                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                              the values array must be empty. This array is replaced during a strategic
                                              merge patch.
                                            items:
                                              type: string
                                            type: array
                                            x-kubernetes-list-type: atomic
                                        required:
                                        - key
                                        - operator
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    matchLabels:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                                      type: object
                                  type: object
                                  x-kubernetes-map-type: atomic
                                namespaces:
                                  description: Namespaces is a list of namespaces
                                    to match.
                                  items:
                                    type: string
                                  type: array
                                resourceNameRegex:
                                  description: ResourceNameRegex is a regular expression
                                    to match the resource name.
                                  type: string
                              required:
                              - groupResource
                              type: object
                            directionPolicy:
                              description: DirectionPolicy gates rule by resolved
                                flow. Default Auto.
                              enum:
                              - Auto
                              - ForwardOnly
                              - ReverseOnly
                              type: string
                            enabled:
                              description: Enabled gates whether rule participates
                                in compilation. Default true.
                              type: boolean
                            id:
                              description: ID is optional user-facing rule id for
                                diagnostics.
                              type: string
                            mode:
                              description: |-
                                Mode controls compilation behavior.
                                Supported values: veleroNative, reversible.
                              enum:
                              - veleroNative
                              - reversible
                              type: string
                            onConflict:
                              description: OnConflict controls same-priority same-key
                                conflict handling. Default Fail.
                              enum:
                              - Fail
                              - Skip
                              type: string
                            pair:
                              description: |-
                                Pair is required when mode=reversible.
                                Pair values may contain restricted placeholders.
                              properties:
                                path:
                                  description: Path is JSON Pointer target path.
                                  minLength: 1
                                  type: string
                                sourceValue:
                                  description: |-
                                    SourceValue is the value for the baseline source side.
                                    The selected value may contain restricted placeholders.
                                  minLength: 1
                                  type: string
                                targetValue:
                                  description: |-
                                    TargetValue is the value for the baseline target side.
                                    The selected value may contain restricted placeholders.
                                  minLength: 1
                                  type: string
                              required:
                              - path
                              - sourceValue
                              - targetValue
                              type: object
                            priority:
                              description: Priority decides conflict precedence, larger
                                wins.
                              format: int32
                              type: integer
                            veleroRule:
                              description: VeleroRule is required when mode=veleroNative.
                              properties:
                                mergePatches:
                                  description: MergePatches is reserved for Phase
                                    2.
                                  items:
                                    type: string
                                  type: array
                                patches:
                                  description: Patches is Velero JSONPatch list (Phase
                                    1 supported).
                                  items:
                                    description: JSONPatch defines a JSON patch operation.
                                    properties:
                                      from:
                                        description: From is the source path (for
                                          "move" or "copy" operations).
                                        type: string
                                      operation:
                                        description: Operation is the type of JSON
                                          patch operation (e.g., "replace", "add",
                                          "remove").
                                        type: string
                                      path:
                                        description: Path is the JSON path to the
                                          field to modify.
                                        type: string
                                      value:
                                        description: Value is the value to set (for
                                          "replace" or "add" operations).
                                        type: string
                                    required:
                                    - operation
                                    - path
                                    type: object
                                  type: array
                                strategicPatches:
                                  description: StrategicPatches is reserved for Phase
                                    2.
                                  items:
                                    type: string
                                  type: array
                              type: object
                          required:
                          - conditions
                          - mode
                          type: object
                        type: array
                      resourceSelection:
                        description: ResourceSelection controls restore include/exclude
                          scope.
                        properties:
                          excludedClusterScopedResources:
                            description: ExcludedClusterScopedResources specifies
                              cluster-scoped resources to exclude.
                            items:
                              type: string
                            type: array
                          excludedNamespaceScopedResources:
                            description: ExcludedNamespaceScopedResources specifies
                              namespace-scoped resources to exclude.
                            items:
                              type: string
                            type: array
                          excludedNamespaces:
                            description: ExcludedNamespaces specifies namespaces to
                              exclude.
                            items:
                              type: string
                            type: array
                          excludedResources:
                            description: ExcludedResources specifies resources to
                              exclude.
                            items:
                              type: string
                            type: array
                          includeClusterResources:
                            description: IncludeClusterResources controls whether
                              cluster-scoped resources are included.
                            type: boolean
                          includedClusterScopedResources:
                            description: IncludedClusterScopedResources specifies
                              cluster-scoped resources to include.
                            items:
                              type: string
                            type: array
                          includedNamespaceScopedResources:
                            description: IncludedNamespaceScopedResources specifies
                              namespace-scoped resources to include.
                            items:
                              type: string
                            type: array
                          includedNamespaces:
                            description: IncludedNamespaces specifies namespaces to
                              include.
                            items:
                              type: string
                            type: array
                          includedResources:
                            description: IncludedResources specifies resources to
                              include.
                            items:
                              type: string
                            type: array
                          labelSelector:
                            description: LabelSelector filters resources by labels.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      storageClassMapping:
                        description: StorageClassMapping defines StorageClass mapping
                          behavior.
                        properties:
                          mappings:
                            description: Mappings defines source->target mapping rules.
                            items:
                              description: RestoreClassMapping defines source->target
                                class mapping for restore.
                              properties:
                                namespaces:
                                  description: Namespaces optionally scopes this mapping
                                    to specific namespaces.
                                  items:
                                    type: string
                                  type: array
                                sourceClass:
                                  description: SourceClass is the class name in source
                                    cluster.
                                  type: string
                                targetClass:
                                  description: TargetClass is the class name to use
                                    in target cluster.
                                  type: string
                              required:
                              - sourceClass
                              - targetClass
                              type: object
                            type: array
                          strictTargetValidation:
                            description: StrictTargetValidation checks whether target
                              classes exist before restore starts.
                            type: boolean
                          unmatchedPolicy:
                            description: |-
                              UnmatchedPolicy controls behavior when mapping is not matched.
                              Supported values: Keep, Fail. Default is Keep.
                            enum:
                            - Keep
                            - Fail
                            type: string
                        type: object
                      useUnifiedDirectionResolver:
                        description: |-
                          UseUnifiedDirectionResolver toggles unified direction resolver + DSL compiler.
                          Default false in Phase 1 for smooth migration.
                        type: boolean
                    type: object
                  skipValidation:
                    description: SkipValidation 跳过前置校验
                    type: boolean
                  targetCluster:
                    description: TargetCluster 演练目标集群 (可选，不指定则使用 Instance 的 secondaryCluster)
                    type: string
                type: object
              force:
                description: Force 是否强制执行操作（跳过不可达的源集群步骤）
                type: boolean
              groupName:
                description: GroupName 要操作的 DisasterGroup 名称 (与 InstanceName 二选一)
                type: string
              instanceName:
                description: InstanceName 要操作的 DisasterInstance 名称 (与 GroupName 二选一)
                type: string
              operationType:
                description: OperationType 要执行的操作类型
                enum:
                - failover
                - reprotect
                - pause
                - resume
                - synconce
                - syncdata
                - syncresource
                - undo
                - cancel
                - drill
                - drill-cleanup
                type: string
              retryPolicy:
                description: RetryPolicy 重试策略
                properties:
                  maxRetries:
                    default: 0
                    description: MaxRetries 最大重试次数，默认为 0（不重试）
                    format: int32
                    type: integer
                  retryIntervalSeconds:
                    default: 5
                    description: RetryIntervalSeconds 重试间隔秒数，默认为 5
                    format: int32
                    type: integer
                type: object
              skipFinalSync:
                description: SkipFinalSync 是否跳过故障切换中的最终同步步骤
                type: boolean
              skipPodReadyCheck:
                description: |-
                  SkipPodReadyCheck 是否跳过本次操作中的 Pod 就绪校验。
                  该字段用于覆盖 DisasterInstance.spec.skipPodReadyCheck 默认策略。
                type: boolean
              skipScaleDownSource:
                description: |-
                  SkipScaleDownSource 是否跳过故障切换中的源集群缩零步骤
                  仅对 operationType=failover 生效
                type: boolean
              timeoutMinutes:
                description: |-
                  TimeoutMinutes 本次操作的超时时间(分钟)。
                  如果未指定，则继承 DisasterInstance 的 OperationTimeoutMinutes。
                format: int32
                type: integer
              waitUntilReady:
                description: |-
                  WaitUntilReady 在故障切换时，是否等待所有 Pod 就绪才认为切换成功。
                  如果为 false，仅检查期望副本数是否下发。如果为 true，会检查 .status.readyReplicas。
                  建议优先使用 SkipPodReadyCheck 进行显式覆盖。
                type: boolean
            required:
            - operationType
            type: object
          status:
            description: DisasterOperationStatus 定义 DisasterOperation 的观测状态
            properties:
              autoCancelCompletionTime:
                description: AutoCancelCompletionTime 自动补偿完成时间
                format: date-time
                type: string
              autoCancelCurrentStep:
                description: AutoCancelCurrentStep 当前执行中的自动补偿步骤
                type: string
              autoCancelMode:
                description: AutoCancelMode 自动补偿分流模式
                type: string
              autoCancelReason:
                description: AutoCancelReason 自动补偿触发原因或最终失败原因
                type: string
              autoCancelStatus:
                description: AutoCancelStatus 自动补偿执行状态
                type: string
              autoCancelSteps:
                description: AutoCancelSteps 自动补偿步骤状态
                items:
                  description: StepStatus 表示单个步骤的状态
                  properties:
                    completionTime:
                      description: CompletionTime 步骤完成时间
                      format: date-time
                      type: string
                    message:
                      description: Message 附加信息
                      type: string
                    name:
                      description: Name 步骤名称
                      type: string
                    startTime:
                      description: StartTime 步骤开始时间
                      format: date-time
                      type: string
                    state:
                      description: State 步骤状态 (Pending/Running/Completed/Failed/Skipped)
                      type: string
                  required:
                  - name
                  - state
                  type: object
                type: array
              autoCancelTriggerStep:
                description: AutoCancelTriggerStep 触发自动补偿的 failover 步骤
                type: string
              autoCancelTriggered:
                description: AutoCancelTriggered 标识 failover 失败后是否触发了自动补偿
                type: boolean
              autoCancelTriggeredAt:
                description: AutoCancelTriggeredAt 自动补偿触发时间
                format: date-time
                type: string
              completionTime:
                description: CompletionTime 操作完成时间
                format: date-time
                type: string
              conditions:
                description: Conditions 最新的观测状态
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentStep:
                description: CurrentStep 当前执行的步骤
                type: string
              dataRestoreName:
                description: DataRestoreName 演练创建的数据恢复 AppRestore 名称 (仅 Drill 操作使用)
                type: string
              groupStatus:
                description: GroupStatus 容灾组操作状态详情
                properties:
                  currentLevelIndex:
                    description: CurrentLevelIndex 当前执行的层级 (0-based)
                    type: integer
                  levelStatuses:
                    description: LevelStatuses 各层详细状态
                    items:
                      description: LevelStatus 记录 Group 操作中每一层的执行状态
                      properties:
                        completionTime:
                          description: CompletionTime 完成时间
                          format: date-time
                          type: string
                        failedInstances:
                          description: FailedInstances 执行失败的实例
                          items:
                            type: string
                          type: array
                        index:
                          description: Index 层级索引 (0-based)
                          type: integer
                        instances:
                          description: Instances 该层包含的实例列表
                          items:
                            type: string
                          type: array
                        startTime:
                          description: StartTime 开始时间
                          format: date-time
                          type: string
                        state:
                          description: State 状态 (Pending, Running, Completed, Failed,
                            TimedOut)
                          type: string
                      required:
                      - index
                      - instances
                      - state
                      type: object
                    type: array
                  totalLevels:
                    description: TotalLevels 总层数
                    type: integer
                required:
                - currentLevelIndex
                - totalLevels
                type: object
              manualInterventionRequired:
                description: ManualInterventionRequired 标识是否仍需人工介入
                type: boolean
              message:
                description: Message 操作的附加信息
                type: string
              nextRetryTime:
                description: NextRetryTime 下次重试时间
                format: date-time
                type: string
              reason:
                description: Reason 机器可读错误码
                type: string
              resourceRestoreName:
                description: ResourceRestoreName 演练创建的资源恢复 AppRestore 名称 (仅 Drill
                  操作使用)
                type: string
              retryCount:
                description: RetryCount 当前已重试次数
                format: int32
                type: integer
              roleStatus:
                description: RoleStatus 操作后的角色分配
                properties:
                  primaryCluster:
                    description: PrimaryCluster 新的主集群
                    type: string
                  secondaryCluster:
                    description: SecondaryCluster 新的备集群
                    type: string
                required:
                - primaryCluster
                - secondaryCluster
                type: object
              startTime:
                description: StartTime 操作开始时间
                format: date-time
                type: string
              state:
                description: State 当前操作状态
                type: string
              steps:
                description: Steps 各步骤的状态
                items:
                  description: StepStatus 表示单个步骤的状态
                  properties:
                    completionTime:
                      description: CompletionTime 步骤完成时间
                      format: date-time
                      type: string
                    message:
                      description: Message 附加信息
                      type: string
                    name:
                      description: Name 步骤名称
                      type: string
                    startTime:
                      description: StartTime 步骤开始时间
                      format: date-time
                      type: string
                    state:
                      description: State 步骤状态 (Pending/Running/Completed/Failed/Skipped)
                      type: string
                  required:
                  - name
                  - state
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: disasterpolicies.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: DisasterPolicy
    listKind: DisasterPolicyList
    plural: disasterpolicies
    singular: disasterpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The type of the policy
      jsonPath: .spec.type
      name: Type
      type: string
    - description: The schedule of the policy
      jsonPath: .spec.schedule
      name: Schedule
      type: string
    - description: The state of the policy
      jsonPath: .spec.state
      name: State
      type: string
    - description: The lifecycle phase of the policy
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: The last time the policy was executed
      jsonPath: .status.lastExecutionTime
      name: LastExecution
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: DisasterPolicy is the Schema for the disasterpolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of DisasterPolicy
            properties:
              description:
                description: Description provides a human-readable description of
                  the policy
                type: string
              schedule:
                description: Schedule is a Cron expression defining when to run the
                  policy
                type: string
              startTime:
                description: StartTime specifies when the policy should start taking
                  effect
                format: date-time
                type: string
              state:
                description: State specifies whether the policy is enabled or disabled
                enum:
                - Enabled
                - Disabled
                type: string
              ttl:
                description: |-
                  TTL defines how long Velero backups created by this AutoBackup policy are retained.
                  Only effective when type is AutoBackup.
                type: string
              type:
                description: Type specifies the type of the policy
                enum:
                - AutoBackup
                - DataSync
                - ResourceSync
                type: string
            required:
            - schedule
            - state
            - type
            type: object
          status:
            description: status defines the observed state of DisasterPolicy
            properties:
              lastEventPhase:
                description: LastEventPhase records the last event phase for debouncing
                type: string
              lastExecutionTime:
                description: LastExecutionTime records the last time the policy was
                  executed
                format: date-time
                type: string
              lastState:
                description: LastState records the last observed policy state (Enabled/Disabled)
                enum:
                - Enabled
                - Disabled
                type: string
              message:
                description: Message provides a detailed message about the current
                  status
                type: string
              nextExecutionTime:
                description: NextExecutionTime records the next scheduled execution
                  time
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration records the generation that was last
                  processed
                format: int64
                type: integer
              phase:
                description: Phase indicates the lifecycle phase of the policy (Active,
                  Deleting)
                enum:
                - Active
                - Deleting
                type: string
              reason:
                description: Reason indicates the reason for the current status
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: resourcesyncs.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: ResourceSync
    listKind: ResourceSyncList
    plural: resourcesyncs
    shortNames:
    - rs
    singular: resourcesync
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: 父级 DisasterInstance
      jsonPath: .spec.instance
      name: Instance
      type: string
    - description: 当前状态
      jsonPath: .status.state
      name: State
      type: string
    - description: 上次同步时间
      jsonPath: .status.lastSyncTime
      name: LastSync
      type: date
    - description: 是否暂停
      jsonPath: .spec.paused
      name: Paused
      type: boolean
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          ResourceSync 是资源同步的 API Schema
          管理 Kubernetes 资源同步（构建"资源骨架"）
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResourceSyncSpec 定义 ResourceSync 的期望状态
            properties:
              excludeResources:
                description: ExcludeResources 需要排除的资源类型列表
                items:
                  type: string
                type: array
              instance:
                description: Instance 父级 DisasterInstance 的名称
                type: string
              paused:
                description: Paused 指示是否暂停同步
                type: boolean
              standbyModifier:
                description: StandbyModifier 定义如何修改资源以实现 Standby 模式
                properties:
                  originalReplicaAnnotation:
                    default: testudo.softcdata.com/original-replica-count
                    description: OriginalReplicaAnnotation 存储原始副本数的注解键
                    type: string
                  scaleToZero:
                    default: true
                    description: ScaleToZero 是否在恢复时将副本数设为 0
                    type: boolean
                type: object
              trigger:
                description: Trigger 定义同步的触发方式
                properties:
                  manual:
                    description: |-
                      Manual 手动触发时间戳
                      设置为当前时间以触发立即同步
                    type: string
                  schedule:
                    description: |-
                      Schedule 定时调度表达式（从 DisasterPolicy 继承）
                      只读字段，由引用的策略填充
                    type: string
                type: object
            required:
            - instance
            type: object
          status:
            description: ResourceSyncStatus 定义 ResourceSync 的观测状态
            properties:
              clusterRestoreStatus:
                description: ClusterRestoreStatus 记录 cluster phase 的 restore 状态
                enum:
                - Pending
                - Restoring
                - InProgress
                - Succeeded
                - Failed
                - Cancelled
                - Initiating
                - Deleting
                - Unknown
                type: string
              conditions:
                description: Conditions 最新的观测状态
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              history:
                description: History 记录最近的同步历史
                items:
                  description: SyncHistoryRecord 定义单次同步的历史记录
                  properties:
                    backupName:
                      description: BackupName 关联的 AppBackup 名称
                      type: string
                    backupResourceCount:
                      description: BackupResourceCount 备份资源数
                      type: integer
                    completionTime:
                      description: CompletionTime 同步结束时间
                      format: date-time
                      type: string
                    duration:
                      description: Duration 同步耗时
                      type: string
                    restoreName:
                      description: RestoreName 关联的 AppRestore 名称 (如果有)
                      type: string
                    restoreResourceCount:
                      description: RestoreResourceCount 恢复资源数
                      type: integer
                    startTime:
                      description: StartTime 同步开始时间
                      format: date-time
                      type: string
                    status:
                      description: Status 同步结果状态 (Completed/Failed)
                      type: string
                  type: object
                type: array
              lastBackupName:
                description: LastBackupName 上次创建的 AppBackup 名称
                type: string
              lastClusterRestoreName:
                description: LastClusterRestoreName 上次创建或观测到的 cluster phase AppRestore
                  名称
                type: string
              lastNamespaceRestoreName:
                description: LastNamespaceRestoreName 上次创建或观测到的 namespaced phase AppRestore
                  名称
                type: string
              lastRestoreName:
                description: LastRestoreName 上次创建的 AppRestore 名称
                type: string
              lastSyncTime:
                description: LastSyncTime 上次同步时间
                format: date-time
                type: string
              message:
                description: Message 人类可读错误描述
                type: string
              namespaceRestoreStatus:
                description: NamespaceRestoreStatus 记录 namespaced phase 的 restore
                  状态
                enum:
                - Pending
                - Restoring
                - InProgress
                - Succeeded
                - Failed
                - Cancelled
                - Initiating
                - Deleting
                - Unknown
                type: string
              reason:
                description: Reason 机器可读错误码
                type: string
              state:
                description: State 当前同步状态
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: storagerepositories.testudo.softcdata.com
spec:
  group: testudo.softcdata.com
  names:
    kind: StorageRepository
    listKind: StorageRepositoryList
    plural: storagerepositories
    singular: storagerepository
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: StorageRepository Status
      jsonPath: .status.status
      name: Status
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: StorageRepository is the Schema for the storagerepositories API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StorageRepository
            properties:
              accessKey:
                type: string
              addressingStyle:
                default: PathStyle
                description: AddressingStyle controls the S3 bucket addressing mode
                  used by validation and runtime.
                enum:
                - PathStyle
                - VirtualHostedStyle
                type: string
              bucket:
                type: string
              caSecretRef:
                description: CASecretRef references a Secret in the same namespace
                  whose `ca.crt` entry contains a custom CA bundle.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              endpoint:
                type: string
              quotaBytes:
                description: QuotaBytes defines the storage limit
                format: int64
                type: integer
              region:
                type: string
              secretKey:
                type: string
              storageType:
                type: string
            required:
            - accessKey
            - bucket
            - endpoint
            - region
            - secretKey
            - storageType
            type: object
          status:
            description: status defines the observed state of StorageRepository
            properties:
              lastCheckTime:
                description: LastCheckTime records the time when the storage was last
                  checked
                format: date-time
                type: string
              lastEventPhase:
                description: LastEventPhase records the last event phase emitted,
                  used to avoid duplicate events
                type: string
              message:
                description: Message provides a detailed message about the current
                  status
                type: string
              observedGeneration:
                description: ObservedGeneration records the generation that was last
                  processed
                format: int64
                type: integer
              observedMetadataHash:
                description: ObservedMetadataHash records the hash of the metadata
                  (labels/annotations) that was last processed
                type: string
              readyTimestamp:
                description: ReadyTimestamp records when the storage became Available,
                  used for Duration calculation
                format: date-time
                type: string
              reason:
                description: Reason indicates the reason for the current status
                type: string
              status:
                type: string
              totalBackupCount:
                description: TotalBackupCount records the physical backup objects
                  count inside S3 based on backups/ prefix
                format: int64
                type: integer
              usedSpaceBytes:
                description: UsedSpaceBytes records the total bytes used by backup
                  objects in the storage repository
                format: int64
                type: integer
            required:
            - status
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
